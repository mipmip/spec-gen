# System Overview

> Generated by spec-gen v1.0.0 on 2026-03-25 23:11
> Confidence: 90%

## Purpose

OpenSpec is a CLI tool designed to generate and manage OpenSpec specifications from codebases. It
provides a comprehensive set of features for analyzing code, generating specifications, verifying
accuracy, and detecting drift between code and specifications.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes code structure to identify potential refactoring opportunities and technical debt indicators. | [spec.md](../analyzer/spec.md) |
| Api | Defines core data models: ProgressEvent, BaseOptions, InitApiOptions and 15 more. | [spec.md](../api/spec.md) |
| Services | Manages reading, writing, and merging configuration files for spec-gen and OpenSpec. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates OpenSpec formatted documentation from analysis results, organizing content by domain and type. | [spec.md](../generator/spec.md) |
| Drift | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Detects discrepancies between code changes and corresponding specifications, identifying potential specification drift. | [spec.md](../drift/spec.md) |
| Verifier | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Verifies the accuracy of OpenSpec specifications against the actual code implementation. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: AnalysisConfig, GenerationConfig, LLMConfig and 8 more. | [spec.md](../types/spec.md) |
| Spec | Return the full content of a spec domain's spec.md plus its mapping entries. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Validates the structure and content of OpenSpec documents and configuration files to ensure compliance with OpenSpec conventions and standards. | [spec.md](../openspec/spec.md) |
| Import | [PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses and analyzes import/export statements in JavaScript/TypeScript files. | [spec.md](../import/spec.md) |
| Chat | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Manages interactions with language model providers and handles tool execution for code analysis tasks. | [spec.md](../chat/spec.md) |
| Stages | Extracts services/modules from business logic files using LLM analysis. | [spec.md](../stages/spec.md) |
| Mcp-handlers | Provides utility functions for MCP tool handlers, including directory validation, error sanitization, and mapping index management. | [spec.md](../mcp-handlers/spec.md) |
| App | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Get the requirement to function mapping produced by spec-gen generate. | [spec.md](../app/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use of layered architecture to separate concerns and improve maintainability
- Integration with OpenAI-compatible LLM APIs for generating specifications
- Use of Git for version control and detecting changes
- Use of file system for reading and writing configuration and analysis data
- Use of HTTP for communication between components and external systems

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI command → API endpoint → service → core component → data processing → response. Data flows from the user input through the CLI, to the API layer, then to the service layer, and finally to the core components for processing. The results are then sent back through the layers to the user.

## Technical Notes

- **Architecture Style**: Layered architecture: CLI layer → API layer → service layer → core layer. This pattern separates concerns and allows for better maintainability and scalability.
- **Security Model**: Authentication and authorization are not explicitly mentioned in the provided data. It is uncertain if the system has any security measures in place.
- **External Integrations**: OpenAI-compatible LLM APIs, Git, File system, HTTP
