# Drift Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 23:11
> Source files: src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, dist/core/drift/spec-mapper.d.ts, dist/core/drift/drift-detector.d.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Detects discrepancies between code
changes and corresponding specifications, identifying potential specification drift.

## Entities

### GitDiffOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for generating git
diffs

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the git repository |
| baseRef | string | Git reference to compare against |
| pathFilter | string[] | Optional paths to include in diff |
| includeUnstaged | boolean | Whether to include unstaged changes |

### GitDiffResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Result of a git diff operation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| resolvedBase | string | The resolved base reference used for comparison |
| files | ChangedFile[] | List of changed files |
| hasUnstagedChanges | boolean | Whether there are unstaged changes |
| currentBranch | string | Name of the current git branch |

**Relationships:**

- contains ChangedFile (Result contains multiple changed files)

### ChangedFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a file that has changed in the
git repository

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the changed file |
| status | string | Change status (added, modified, deleted, renamed) |
| oldPath | string | Original path if file was renamed |
| isTest | boolean | Whether the file is a test file |
| isConfig | boolean | Whether the file is a configuration file |
| isGenerated | boolean | Whether the file is generated code |
| extension | string | File extension |

### GitDiff

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the diff information between
the current working tree and a base reference in a Git repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | The root directory of the Git repository |
| baseRef | string | The base reference (branch, commit, or tag) to compare against |
| includeUnstaged | boolean | Whether to include unstaged changes in the diff |
| maxChars | number | Maximum number of characters to include in the diff output |

**Relationships:**

- contains ChangedFile (A GitDiff contains multiple ChangedFile entities)

### SpecMapping

Represents the mapping between a specification domain and its associated source files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | The name of the domain |
| specPath | string | Path to the specification file |
| declaredSourceFiles | string[] | Source files explicitly declared in the spec header |
| inferredSourceFiles | string[] | Source files inferred from the spec content |
| allSourceFiles | string[] | Combined list of declared and inferred source files |
| requirements | string[] | List of requirements extracted from the spec |
| entities | string[] | List of entities mentioned in the spec |

### SpecMap

Represents the bidirectional mapping between specification domains and source files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDomain | Map<string, SpecMapping> | Mapping from domain names to their specifications |
| byFile | Map<string, string[]> | Mapping from source files to their associated domains |
| domainCount | number | Total number of domains in the mapping |
| totalMappedFiles | number | Total number of source files mapped |

**Relationships:**

- contains SpecMapping (SpecMap contains multiple SpecMapping instances)

### SpecMapperOptions

Configuration options for the SpecMapper.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root path of the project |
| openspecPath | string | Path to the OpenSpec directory |
| repoStructurePath | string | Optional path to the repository structure file |

### RepoStructureDomain

Represents a domain in the repository structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| files | string[] | List of files associated with the domain |
| suggestedSpecPath | string | Suggested path for the specification file |

### ADRMapping

Represents a mapping between an Architecture Decision Record (ADR) and related domains and layers.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the ADR |
| title | string | Title of the ADR |
| adrPath | string | Path to the ADR file |
| relatedDomains | string[] | List of domains related to the ADR |
| relatedLayers | string[] | List of layers related to the ADR |
| status | string | Status of the ADR |

**Relationships:**

- part-of ADRMap (ADRMapping is part of an ADRMap)

### ADRMap

A map of Architecture Decision Records (ADRs) by their ID and related domains.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byId | Map<string, ADRMapping> | Map of ADR mappings by their ID |
| byDomain | Map<string, string[]> | Map of ADR IDs by related domain |

**Relationships:**

- contains ADRMapping (ADRMap contains multiple ADRMapping instances)

### DriftDetectorOptions

Configuration options for the drift detection engine

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory path of the project |
| specMap | SpecMap | Mapping of specifications to their domains |
| changedFiles | ChangedFile[] | List of files that have changed |
| failOn | DriftSeverity | Severity level that will cause the detection to fail |
| llm | LLMService | Optional LLM service for enhanced drift detection |
| domainFilter | string[] | Optional list of domains to filter detection on |
| openspecRelPath | string | Relative path to the openspec directory |
| baseRef | string | Git base reference for fetching file diffs |
| maxLlmCalls | number | Maximum number of LLM calls per run |
| adrMap | ADRMap | Optional map of architectural decision records |

**Relationships:**

- uses SpecMap (Uses a SpecMap to determine specification coverage)
- uses ChangedFile (Analyzes changes in ChangedFile objects)
- uses LLMService (Optionally uses LLMService for enhanced analysis)
- uses ADRMap (Optionally uses ADRMap for ADR drift detection)

### DriftIssue

Represents a detected drift issue in the codebase

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| kind | DriftIssueKind | Type of drift issue detected |
| severity | DriftSeverity | Severity level of the drift issue |
| message | string | Description of the drift issue |
| filePath | string | Path to the file related to the issue |
| domain | string | Domain associated with the issue |
| specPath | string | Path to the specification file related to the issue |
| adrId | string | ID of the ADR related to the issue |

**Relationships:**

- uses DriftIssueKind (Uses DriftIssueKind to categorize the issue)
- uses DriftSeverity (Uses DriftSeverity to determine the severity level)

## Requirements

### Requirement: GitDiffOptionsValidation

The system SHALL validate GitDiffOptions according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid git reference
- includeUnstaged must be a boolean

#### Scenario: ConfigureGitDiffOptions
- **GIVEN** A valid git repository path
- **WHEN** GitDiffOptions is created with valid parameters
- **THEN** Options are properly configured for diff generation

### Requirement: GitDiffResultValidation

The system SHALL validate GitDiffResult according to these rules:
- resolvedBase must be a valid git reference
- files must be an array of ChangedFile objects
- hasUnstagedChanges must be a boolean
- currentBranch must be a string

#### Scenario: GenerateGitDiffResult
- **GIVEN** A valid git repository with changes
- **WHEN** gitDiff is called with valid options
- **THEN** Returns a GitDiffResult with the current changes

### Requirement: ChangedFileValidation

The system SHALL validate ChangedFile according to these rules:
- path must be a valid file path
- status must be one of: added, modified, deleted, renamed
- oldPath must be a valid file path if status is renamed
- isTest, isConfig, isGenerated must be booleans
- extension must be a valid file extension

#### Scenario: ClassifyChangedFile
- **GIVEN** A file path that has changed
- **WHEN** classifyFile is called with the file path
- **THEN** Returns file classification properties

### Requirement: GitDiffValidation

The system SHALL validate GitDiff according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid Git reference (branch, commit, or tag)
- maxChars must be a positive integer

#### Scenario: GetChangedFiles
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getChangedFiles() is called with valid options
- **THEN** Returns a list of ChangedFile entities with their respective changes

#### Scenario: GetFileDiff
- **GIVEN** A specific file that has been modified in the working tree
- **WHEN** getFileDiff() is called with the file path and base reference
- **THEN** Returns the unified diff content for the specified file

### Requirement: ADRMappingValidation

The system SHALL validate ADRMapping according to these rules:
- id must be a unique string
- adrPath must be a valid file path

#### Scenario: ParseADRRelated
- **GIVEN** An ADR file content with a ## Related section
- **WHEN** parseADRRelated() is called
- **THEN** The related domains and layers are extracted from the content

### Requirement: ADRMapValidation

The system SHALL validate ADRMap according to these rules:
- byId must be a valid Map with string keys and ADRMapping values
- byDomain must be a valid Map with string keys and string[] values

#### Scenario: BuildADRMap
- **GIVEN** A valid SpecMapperOptions with rootPath and openspecPath
- **WHEN** buildADRMap() is called
- **THEN** An ADRMap is created with mappings of ADRs by ID and related domains

### Requirement: DriftDetectorOptionsValidation

The system SHALL validate DriftDetectorOptions according to these rules:
- rootPath must be a valid directory path
- specMap must be a valid SpecMap object
- changedFiles must be a non-empty array of ChangedFile objects
- failOn must be a valid DriftSeverity value
- maxLlmCalls must be a positive integer if provided

#### Scenario: ConfigureDriftDetection
- **GIVEN** A project with specifications and changed files
- **WHEN** DriftDetectorOptions is configured
- **THEN** The drift detection engine is properly initialized with the provided options

### Requirement: DriftIssueValidation

The system SHALL validate DriftIssue according to these rules:
- kind must be a valid DriftIssueKind value
- severity must be a valid DriftSeverity value
- message must be a non-empty string

#### Scenario: CreateADriftIssue
- **GIVEN** A detected drift in the codebase
- **WHEN** A DriftIssue is created
- **THEN** The issue is properly categorized and described

### Requirement: GetChangedFiles

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL retrieves a list of changed files between the base reference and the current state of the repository.

#### Scenario: ValidRepositoryWithChanges
- **GIVEN** A Git repository with both staged and unstaged changes
- **WHEN** getChangedFiles is called with includeUnstaged=true
- **THEN** Returns a list of all changed files including unstaged changes

#### Scenario: SingleCommitRepository
- **GIVEN** A Git repository with only one commit
- **WHEN** getChangedFiles is called with baseRef='auto'
- **THEN** Returns all files as 'added' using the empty tree SHA

### Requirement: ClassifyFile

> Implementation: `src/core/drift/git-diff.ts:66` · confidence: llm

The system SHALL classifies a file as test, config, generated, or regular based on its path and extension.

#### Scenario: TestFileInTestDirectory
- **GIVEN** A file path containing '/test/'
- **WHEN** classifyFile is called
- **THEN** Returns isTest=true

#### Scenario: GeneratedTypeScriptFile
- **GIVEN** A file path ending with '.generated.ts'
- **WHEN** classifyFile is called
- **THEN** Returns isGenerated=true

### Requirement: IsSkippableFile

> Implementation: `src/core/drift/git-diff.ts:93` · confidence: llm

The system SHALL determines if a file should be skipped based on its extension or filename.

#### Scenario: BinaryFile
- **GIVEN** A file path ending with '.png'
- **WHEN** isSkippableFile is called
- **THEN** Returns true

#### Scenario: RegularSourceFile
- **GIVEN** A file path ending with '.ts'
- **WHEN** isSkippableFile is called
- **THEN** Returns false

### Requirement: IsGitRepository

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL checks if the given path is a git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A directory containing a .git subdirectory
- **WHEN** isGitRepository is called
- **THEN** Returns true

#### Scenario: NonGitDirectory
- **GIVEN** A directory without a .git subdirectory
- **WHEN** isGitRepository is called
- **THEN** Returns false

### Requirement: GetCurrentBranch

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL retrieves the name of the current git branch.

#### Scenario: ValidRepository
- **GIVEN** A Git repository with a known branch
- **WHEN** getCurrentBranch is called
- **THEN** Returns the current branch name

#### Scenario: DetachedHEADState
- **GIVEN** A Git repository in detached HEAD state
- **WHEN** getCurrentBranch is called
- **THEN** Returns 'unknown'

### Requirement: ValidateGitRef

> Implementation: `src/core/drift/git-diff.ts:116` · confidence: llm

The system SHALL validates a git reference to prevent argument injection.

#### Scenario: ValidBranchName
- **GIVEN** A valid branch name 'main'
- **WHEN** validateGitRef is called
- **THEN** Does not throw an error

#### Scenario: InvalidBranchName
- **GIVEN** A branch name containing shell metacharacters
- **WHEN** validateGitRef is called
- **THEN** Throws an error

### Requirement: ResolveBaseRef

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL resolves a base git reference, falling back to common defaults if needed.

#### Scenario: ValidPreferredRef
- **GIVEN** A valid Git reference 'main'
- **WHEN** resolveBaseRef is called
- **THEN** Returns 'main'

#### Scenario: SingleCommitRepository
- **GIVEN** A Git repository with only one commit
- **WHEN** resolveBaseRef is called with preferredRef='auto'
- **THEN** Returns the empty tree SHA

### Requirement: ParseSpecHeader

> Implementation: `src/core/drift/spec-mapper.ts:14` · confidence: llm

The system SHALL extracts source file references and generation date from the header of an openspec specification.

#### Scenario: ValidHeaderWithSourceFiles
- **GIVEN** A spec header containing source file references
- **WHEN** parseSpecHeader is called
- **THEN** The source files and generation date are extracted correctly

#### Scenario: HeaderWithoutSourceFiles
- **GIVEN** A spec header without source file references
- **WHEN** parseSpecHeader is called
- **THEN** An empty array of source files and null generation date are returned

### Requirement: ParseSpecReferences

> Implementation: `src/core/drift/spec-mapper.ts:38` · confidence: llm

The system SHALL extracts file references, requirements, and entities from the body of an openspec specification.

#### Scenario: SpecWithImplementationBlock
- **GIVEN** A spec containing an implementation block with file references
- **WHEN** parseSpecReferences is called
- **THEN** The file references, requirements, and entities are extracted correctly

#### Scenario: SpecWithoutImplementationBlock
- **GIVEN** A spec without an implementation block
- **WHEN** parseSpecReferences is called
- **THEN** An empty array of files, requirements, and entities are returned

### Requirement: InferDomainFromPath

> Implementation: `src/core/drift/spec-mapper.ts:83` · confidence: llm

The system SHALL infers a domain for a file path based on directory structure and known domains.

#### Scenario: FilePathMatchesKnownDomain
- **GIVEN** A file path containing a known domain
- **WHEN** inferDomainFromPath is called
- **THEN** The correct domain is inferred

#### Scenario: FilePathDoesNotMatchKnownDomain
- **GIVEN** A file path not containing any known domains
- **WHEN** inferDomainFromPath is called
- **THEN** Null is returned

### Requirement: BuildSpecMap

> Implementation: `src/core/drift/index.ts:7` · confidence: llm

The system SHALL builds a bidirectional map between spec domains and source files.

#### Scenario: ValidSpecDirectory
- **GIVEN** A directory containing valid OpenSpec specifications
- **WHEN** buildSpecMap is called
- **THEN** A bidirectional map is built successfully

#### Scenario: InvalidSpecDirectory
- **GIVEN** A directory without any OpenSpec specifications
- **WHEN** buildSpecMap is called
- **THEN** An empty map is returned

### Requirement: GetSpecContent

> Implementation: `src/core/drift/index.ts:7` · confidence: llm

The system SHALL loads and truncates the content of a spec file for a given domain.

#### Scenario: ValidSpecFile
- **GIVEN** A valid spec file for the given domain
- **WHEN** getSpecContent is called
- **THEN** The content of the spec file is loaded and truncated if necessary

#### Scenario: InvalidSpecFile
- **GIVEN** An invalid spec file for the given domain
- **WHEN** getSpecContent is called
- **THEN** Null is returned

### Requirement: GetFileDiff

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL retrieves the diff content for a specific file between the working tree and a base reference.

#### Scenario: SuccessfulThreeDotDiff
- **GIVEN** A valid base reference and a file with changes
- **WHEN** getFileDiff is called with a three-dot separator
- **THEN** The diff content is returned
- **AND** The diff content is truncated if it exceeds maxChars

#### Scenario: FailedThreeDotDiff
- **GIVEN** A base reference with no common ancestor
- **WHEN** getFileDiff is called with a three-dot separator
- **THEN** The two-dot diff is attempted
- **AND** The diff content is returned if successful

#### Scenario: FailedTwoDotDiff
- **GIVEN** A base reference with no changes
- **WHEN** getFileDiff is called with a two-dot separator
- **THEN** The unstaged/staged diff is attempted
- **AND** The diff content is returned if successful

#### Scenario: NoChanges
- **GIVEN** A file with no changes
- **WHEN** getFileDiff is called
- **THEN** An empty string is returned

### Requirement: GetChangedFiles

> Implementation: `src/core/drift/index.ts:1` · confidence: llm

The system SHALL retrieves a list of changed files between the working tree and a base reference.

#### Scenario: SuccessfulThreeDotDiff
- **GIVEN** A valid base reference and changed files
- **WHEN** getChangedFiles is called with a three-dot separator
- **THEN** A list of changed files is returned
- **AND** The list includes line-level stats for each file

#### Scenario: FailedThreeDotDiff
- **GIVEN** A base reference with no common ancestor
- **WHEN** getChangedFiles is called with a three-dot separator
- **THEN** The two-dot diff is attempted
- **AND** A list of changed files is returned if successful

#### Scenario: IncludeUnstagedChanges
- **GIVEN** Unstaged changes in the working tree
- **WHEN** getChangedFiles is called with includeUnstaged set to true
- **THEN** The list of changed files includes unstaged changes
- **AND** The hasUnstagedChanges flag is set to true

#### Scenario: PathFilterApplied
- **GIVEN** A path filter is provided
- **WHEN** getChangedFiles is called
- **THEN** Only files matching the path filter are returned

### Requirement: IsSpecRelevantChange

> Implementation: `src/core/drift/drift-detector.ts:32` · confidence: llm

The system SHALL determines if a changed file is relevant for specification drift detection by filtering out non-relevant files.

#### Scenario: TestFile
- **GIVEN** A file marked as a test file
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns false

#### Scenario: GeneratedFile
- **GIVEN** A file marked as generated
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns false

#### Scenario: SpecFile
- **GIVEN** A file located in the openspec directory
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns false

### Requirement: ComputeSeverity

> Implementation: `src/core/drift/drift-detector.ts:69` · confidence: llm

The system SHALL computes the severity of a drift issue based on the issue kind and change magnitude.

#### Scenario: HighValueFileWithSignificantChanges
- **GIVEN** A high-value file with more than 30 lines changed
- **WHEN** computeSeverity is called with kind 'gap'
- **THEN** Returns 'error'

#### Scenario: DeletedFile
- **GIVEN** A file with status 'deleted'
- **WHEN** computeSeverity is called with kind 'stale'
- **THEN** Returns 'error'

### Requirement: ExtractChangedSpecDomains

> Implementation: `src/core/drift/drift-detector.ts:80` · confidence: llm

The system SHALL extracts domain names whose specifications were also changed in the changeset.

#### Scenario: SpecFileChanged
- **GIVEN** A file located in the openspec directory
- **WHEN** extractChangedSpecDomains is called
- **THEN** Adds the domain name to the set of changed domains

### Requirement: DetectGaps

> Implementation: `src/core/drift/drift-detector.ts:94` · confidence: llm

The system SHALL detects gaps where code changed in a domain but the specification wasn't updated.

#### Scenario: SpecUpdated
- **GIVEN** A file with a domain that has been updated in the specification
- **WHEN** detectGaps is called
- **THEN** Does not create a gap issue

#### Scenario: SpecNotUpdated
- **GIVEN** A file with a domain that has not been updated in the specification
- **WHEN** detectGaps is called
- **THEN** Creates a gap issue

## Technical Notes

- **Implementation**: `src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, dist/core/drift/spec-mapper.d.ts, dist/core/drift/drift-detector.d.ts`
- **Dependencies**: ChangedFile, logger, SpecMapping, SpecMap, RepoStructureDomain, node:child_process, node:fs/promises, node:path, node:util, DIFF_MAX_CHARS, SpecMapper, GitDiff, Logger

## Dependencies

### Called by this domain
- `api` → `getChangedFiles`, `isGitRepository`, `buildSpecMap`
- `commands` → `getChangedFiles`, `isGitRepository`, `buildSpecMap`
- `mcp-handlers` → `isGitRepository`, `getChangedFiles`, `buildSpecMap`

### Calls into
- `services` → `LLMService`
- `types` → `ChangedFile`, `SpecMapping`, `SpecMap`
- `utilities` → `logger`
