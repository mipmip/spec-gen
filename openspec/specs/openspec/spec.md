# Openspec Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 23:11

## Purpose

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Validates the structure and content of
OpenSpec documents and configuration files to ensure compliance with OpenSpec conventions and
standards.

## Requirements

### Requirement: ValidateSpecStructure

> Implementation: `src/core/generator/openspec-format-generator.ts:906` · confidence: semantic

The system SHALL validates the overall structure of an openspec document, including headings, sections, and markdown formatting.

#### Scenario: ValidSpecStructure
- **GIVEN** A properly formatted OpenSpec document
- **WHEN** validateSpecStructure is called
- **THEN** Returns a ValidationResult with valid: true and no errors

#### Scenario: MissingTitle
- **GIVEN** An OpenSpec document without a title (# heading)
- **WHEN** validateSpecStructure is called
- **THEN** Returns a ValidationResult with valid: false and an error message about the missing title

#### Scenario: MalformedMarkdownLinks
- **GIVEN** An OpenSpec document with malformed markdown links
- **WHEN** validateSpecStructure is called
- **THEN** Returns a ValidationResult with valid: false and warnings about the malformed links

### Requirement: ValidateRequirementFormat

> Implementation: `src/core/drift/spec-mapper.ts:38` · confidence: semantic

The system SHALL validates that requirements follow rfc 2119 keywords and are properly formatted.

#### Scenario: ValidRequirementFormat
- **GIVEN** A requirement using RFC 2119 keywords and in imperative form
- **WHEN** validateRequirementFormat is called
- **THEN** Returns a ValidationResult with valid: true and no warnings

#### Scenario: MissingRFC2119Keywords
- **GIVEN** A requirement without RFC 2119 keywords
- **WHEN** validateRequirementFormat is called
- **THEN** Returns a ValidationResult with valid: true and a warning about missing RFC 2119 keywords

#### Scenario: TooBriefRequirement
- **GIVEN** A requirement that is too brief
- **WHEN** validateRequirementFormat is called
- **THEN** Returns a ValidationResult with valid: true and a warning about the requirement being too brief

### Requirement: ValidateScenarioFormat

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL validates that scenarios follow the given/when/then format with proper markdown formatting.

#### Scenario: ValidScenarioFormat
- **GIVEN** A scenario with properly formatted Given/When/Then clauses
- **WHEN** validateScenarioFormat is called
- **THEN** Returns a ValidationResult with valid: true and no errors

#### Scenario: MissingGivenClause
- **GIVEN** A scenario without a Given clause
- **WHEN** validateScenarioFormat is called
- **THEN** Returns a ValidationResult with valid: false and an error message about the missing Given clause

#### Scenario: IncorrectANDClauseFormatting
- **GIVEN** A scenario with an AND clause that is not bolded
- **WHEN** validateScenarioFormat is called
- **THEN** Returns a ValidationResult with valid: true and a warning about the incorrect AND clause formatting

### Requirement: ValidateConfigYaml

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL validates the structure and content of a config.yaml file to ensure it meets openspec requirements.

#### Scenario: ValidConfig.yaml
- **GIVEN** A properly formatted config.yaml file
- **WHEN** validateConfigYaml is called
- **THEN** Returns a ValidationResult with valid: true and no errors

#### Scenario: InvalidSchemaField
- **GIVEN** A config.yaml file with an invalid schema field
- **WHEN** validateConfigYaml is called
- **THEN** Returns a ValidationResult with valid: false and an error message about the invalid schema field

#### Scenario: UnknownRuleKey
- **GIVEN** A config.yaml file with an unknown rule key
- **WHEN** validateConfigYaml is called
- **THEN** Returns a ValidationResult with valid: true and a warning about the unknown rule key

### Requirement: BuildContext

> Implementation: `src/core/generator/openspec-compat.ts:374` · confidence: llm

The system SHALL builds detected context from analysis results.

#### Scenario: ContextBuilt
- **GIVEN** Valid survey object
- **WHEN** buildDetectedContext is called
- **THEN** Returns the detected context object

### Requirement: Normalize

> Implementation: `src/core/generator/openspec-compat.ts:427` · confidence: llm

The system SHALL normalizes a domain name to openspec conventions.

#### Scenario: NameNormalized
- **GIVEN** Valid domain name
- **WHEN** normalizeDomainName is called
- **THEN** Returns the normalized domain name

### Requirement: Validate

> Implementation: `src/core/generator/openspec-compat.ts:434` · confidence: llm

The system SHALL checks if a domain name follows openspec conventions.

#### Scenario: ValidName
- **GIVEN** Domain name follows OpenSpec conventions
- **WHEN** isValidDomainName is called
- **THEN** Returns true

#### Scenario: InvalidName
- **GIVEN** Domain name does not follow OpenSpec conventions
- **WHEN** isValidDomainName is called
- **THEN** Returns false

### Requirement: Match

> Implementation: `src/core/generator/openspec-compat.ts:443` · confidence: llm

The system SHALL matches suggested domains against existing domains.

#### Scenario: DomainsMatched
- **GIVEN** Valid suggestedDomains and existingDomains arrays
- **WHEN** matchExistingDomains is called
- **THEN** Returns a Map of matched domains

## Technical Notes

- **Dependencies**: node:fs/promises, node:path, yaml, logger, constants
