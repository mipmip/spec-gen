# API Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 23:11

## Purpose

This document specifies the HTTP API exposed by the system.

## Requirements

### Requirement: APIAuthentication

The API SHALL require authentication via: None, Not specified, API keys (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY), Bearer JWT, API key (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY), None (CLI command), Not applicable (CLI command), Environment variables (ANTHROPIC_API_KEY or OPENAI_API_KEY), API keys (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, OPENAI_COMPAT_API_KEY), API Key (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY)

#### Scenario: AuthenticatedRequest
- **GIVEN** a request with valid authentication credentials
- **WHEN** the request is processed
- **THEN** the request is authenticated successfully

#### Scenario: UnauthenticatedRequest
- **GIVEN** a request without authentication
- **WHEN** accessing a protected endpoint
- **THEN** the response status is 401 Unauthorized

### Requirement: FunctionInitialization

The API SHALL support `Function init(options: InitApiOptions): Promise<InitResult>` to initialize the specification generation process by creating a configuration file and setting up the necessary directories.

**Request:**

```json
{
  "force": "boolean",
  "openspecPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string",
  "created": "boolean"
}
```

#### Scenario: InitializeWithDefaultSettings
- **GIVEN** No existing configuration
- **WHEN** init() is called with default options
- **THEN** A new configuration file is created and the openspec directory is set up

#### Scenario: ForceInitialization
- **GIVEN** An existing configuration
- **WHEN** init() is called with force: true
- **THEN** The existing configuration is overwritten and a new one is created

### Requirement: FunctionAnalysis

The API SHALL support `Function analyze(options: AnalyzeApiOptions): Promise<AnalyzeResult>` to analyze the project source code to generate a repository map, dependency graph, and analysis artifacts.

**Request:**

```json
{
  "maxFiles": "number",
  "includePatterns": "string[]",
  "excludePatterns": "string[]",
  "force": "boolean",
  "outputPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "repoMap": "CoreRepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: AnalyzeProjectWithDefaultSettings
- **GIVEN** A project with source code
- **WHEN** analyze() is called with default options
- **THEN** A repository map, dependency graph, and analysis artifacts are generated

#### Scenario: ForceReAnalysis
- **GIVEN** An existing analysis
- **WHEN** analyze() is called with force: true
- **THEN** A fresh analysis is performed and the existing analysis is overwritten

### Requirement: FunctionAnalysis

The API SHALL support `Function specGenAnalyze(options: AnalyzeApiOptions = {})` to run static analysis on the codebase. scans the repository, builds a dependency graph, and generates analysis artifacts. no llm involvement.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "maxFiles": "number",
    "excludePatterns": "string[]",
    "includePatterns": "string[]",
    "force": "boolean",
    "outputPath": "string",
    "onProgress": "ProgressCallback"
  }
}
```

**Response:**

```json
{
  "repoMap": "object",
  "depGraph": "DependencyGraphResult",
  "artifacts": {
    "repoStructure": "RepoStructure",
    "summaryMarkdown": "string",
    "dependencyDiagram": "string",
    "llmContext": "LLMContext"
  },
  "duration": "number"
}
```

#### Scenario: AnalyzeWithDefaultOptions
- **GIVEN** No options provided
- **WHEN** specGenAnalyze() is called
- **THEN** Returns analysis results with default settings

#### Scenario: AnalyzeWithCustomOptions
- **GIVEN** Custom options provided
- **WHEN** specGenAnalyze(options) is called
- **THEN** Returns analysis results with custom settings

#### Scenario: AnalyzeWithExistingRecentAnalysis
- **GIVEN** Recent analysis exists and force is false
- **WHEN** specGenAnalyze() is called
- **THEN** Returns cached analysis results

### Requirement: POSTAnalysis

The API SHALL support `POST /api/analyze` to [partial spec — file too large to fully analyze (3 parts)] run the complete analysis pipeline on a codebase.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "options": {
    "maxFiles": "number",
    "include": "string[]",
    "exclude": "string[]"
  }
}
```

**Response:**

```json
{
  "repoMap": "RepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: RunAnalysis
- **GIVEN** Authenticated user with valid codebase path
- **WHEN** POST /api/analyze is called with valid parameters
- **THEN** 200 OK with analysis results in response body
- **AND** Repository mapping is performed
- **AND** Dependency graph is built
- **AND** Analysis artifacts are generated

#### Scenario: InvalidParameters
- **GIVEN** Authenticated user with invalid codebase path
- **WHEN** POST /api/analyze is called with invalid parameters
- **THEN** 400 Bad Request with error message in response body

### Requirement: FunctionAnalysis

The API SHALL support `Function getAnalysisAge(analysisPath: string)` to [partial spec — file too large to fully analyze (2 parts)] get the age of the analysis data if it exists.

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "age": "number | null"
}
```

#### Scenario: GetAnalysisAge
- **GIVEN** Valid analysis path
- **WHEN** Function is called
- **THEN** Returns the age of the analysis data or null if it doesn't exist

### Requirement: FunctionAnalysis

The API SHALL support `Function loadAnalysis(analysisPath)` to [partial spec — file too large to fully analyze (2 parts)] load analysis data from disk including repository structure, llm context, dependency graph, and analysis age.

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "llmContext": "object",
  "depGraph": "object",
  "age": "number",
  "timestamp": "string"
}
```

#### Scenario: LoadAnalysisData
- **GIVEN** Valid analysis path with required files
- **WHEN** Function is called with analysisPath
- **THEN** Returns analysis data including repoStructure, llmContext, depGraph, age, and timestamp

### Requirement: FunctionAnalysis

The API SHALL support `Function getAnalysisAge(analysisPath)` to [partial spec — file too large to fully analyze (2 parts)] get the age of the analysis data if it exists.

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "age": "number | null"
}
```

#### Scenario: GetAnalysisAge
- **GIVEN** An analysis path
- **WHEN** The function is called
- **THEN** Returns the age of the analysis data or null if it doesn't exist

### Requirement: FunctionAnalysis

The API SHALL support `Function checkAnalysis` to [partial spec — file too large to fully analyze (2 parts)] check if the analysis artifacts exist and are not stale.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts exist and are not stale
- **WHEN** checkAnalysis is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating the analysis artifacts exist and are fresh

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts exist but are stale
- **WHEN** checkAnalysis is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating the analysis artifacts are stale and a fix message

#### Scenario: AnalysisCheck
- **GIVEN** The analysis artifacts do not exist
- **WHEN** checkAnalysis is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating no analysis found and a fix message

### Requirement: FunctionGeneration

The API SHALL support `Function generate(options: GenerateApiOptions): Promise<GenerateResult>` to generate openapi specifications and architecture decision records (adrs) based on the analyzed project.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "domains": "string[]",
  "writeMode": "'replace' | 'merge' | 'skip'",
  "adr": "boolean",
  "adrOnly": "boolean",
  "mapping": "boolean",
  "dryRun": "boolean",
  "analysisPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "GenerationReport",
  "pipelineResult": "PipelineResult",
  "duration": "number"
}
```

#### Scenario: GenerateSpecificationsWithDefaultSettings
- **GIVEN** An analyzed project
- **WHEN** generate() is called with default options
- **THEN** OpenAPI specifications and ADRs are generated and saved

#### Scenario: DryRunGeneration
- **GIVEN** An analyzed project
- **WHEN** generate() is called with dryRun: true
- **THEN** Specifications and ADRs are generated but not saved

### Requirement: FunctionVerification

The API SHALL support `Function verify(options: VerifyApiOptions): Promise<VerifyResult>` to verify the generated specifications against the project source code to ensure accuracy.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "samples": "number",
  "threshold": "number",
  "domains": "string[]",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "duration": "number"
}
```

#### Scenario: VerifySpecificationsWithDefaultSettings
- **GIVEN** Generated specifications
- **WHEN** verify() is called with default options
- **THEN** Specifications are verified against the source code

#### Scenario: VerifySpecificDomains
- **GIVEN** Generated specifications
- **WHEN** verify() is called with domains specified
- **THEN** Only the specified domains are verified

### Requirement: FunctionDriftDetection

The API SHALL support `Function drift(options: DriftApiOptions): Promise<DriftResult>` to detect drift between the generated specifications and the current state of the project.

**Request:**

```json
{
  "baseRef": "string",
  "files": "string[]",
  "domains": "string[]",
  "llmEnhanced": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "failOn": "DriftSeverity",
  "maxFiles": "number",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "drift": "DriftResult"
}
```

#### Scenario: DetectDriftWithDefaultSettings
- **GIVEN** Generated specifications and current project state
- **WHEN** drift() is called with default options
- **THEN** Drift between specifications and project state is detected

#### Scenario: LLMEnhancedDriftDetection
- **GIVEN** Generated specifications and current project state
- **WHEN** drift() is called with llmEnhanced: true
- **THEN** Drift is detected using LLM for deeper semantic comparison

### Requirement: POSTDriftDetection

The API SHALL support `POST /api/drift` to detect spec drift in a project by comparing code changes against existing openspec specifications and reporting gaps, stale specs, uncovered files, and orphaned specs.

**Request:**

```json
{
  "rootPath": "string",
  "baseRef": "string",
  "files": "string[]",
  "domains": "string[]",
  "llmEnhanced": "boolean",
  "failOn": "string",
  "maxFiles": "number",
  "onProgress": "function",
  "provider": "string",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean"
}
```

**Response:**

```json
{
  "timestamp": "string",
  "baseRef": "string",
  "totalChangedFiles": "number",
  "specRelevantFiles": "number",
  "issues": "array",
  "summary": {
    "gaps": "number",
    "stale": "number",
    "uncovered": "number",
    "orphanedSpecs": "number",
    "adrGaps": "number",
    "adrOrphaned": "number",
    "total": "number"
  },
  "hasDrift": "boolean",
  "duration": "number",
  "mode": "string"
}
```

#### Scenario: DetectDriftWithLLMEnhancement
- **GIVEN** A project with code changes and existing OpenSpec specifications
- **WHEN** POST /api/drift is called with llmEnhanced set to true
- **THEN** 200 OK with drift detection results including LLM-enhanced analysis

#### Scenario: DetectDriftWithoutLLMEnhancement
- **GIVEN** A project with code changes and existing OpenSpec specifications
- **WHEN** POST /api/drift is called with llmEnhanced set to false
- **THEN** 200 OK with drift detection results without LLM enhancement

#### Scenario: HandleNonGitRepository
- **GIVEN** A project that is not a git repository
- **WHEN** POST /api/drift is called
- **THEN** 400 Bad Request with error message 'Not a git repository. Drift detection requires git.'

#### Scenario: HandleMissingSpecGenConfiguration
- **GIVEN** A project without spec-gen configuration
- **WHEN** POST /api/drift is called
- **THEN** 400 Bad Request with error message 'No spec-gen configuration found. Run specGenInit() first.'

#### Scenario: HandleMissingSpecs
- **GIVEN** A project without existing OpenSpec specifications
- **WHEN** POST /api/drift is called
- **THEN** 400 Bad Request with error message 'No specs found. Run specGenGenerate() first.'

#### Scenario: HandleMissingLLMAPIKey
- **GIVEN** A project with llmEnhanced set to true but no LLM API key
- **WHEN** POST /api/drift is called
- **THEN** 400 Bad Request with error message 'No LLM API key found. LLM-enhanced drift requires
ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY.'

### Requirement: FunctionFullPipeline

The API SHALL support `Function run(options: RunApiOptions): Promise<RunResult>` to run the full specification generation pipeline from initialization to verification.

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "maxFiles": "number",
  "adr": "boolean",
  "dryRun": "boolean",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "init": "InitResult",
  "analysis": "AnalyzeResult",
  "generation": "GenerateResult",
  "duration": "number"
}
```

#### Scenario: RunFullPipelineWithDefaultSettings
- **GIVEN** A project with source code
- **WHEN** run() is called with default options
- **THEN** The full pipeline from initialization to verification is executed

#### Scenario: DryRunFullPipeline
- **GIVEN** A project with source code
- **WHEN** run() is called with dryRun: true
- **THEN** The full pipeline is executed but no changes are saved

### Requirement: FunctionInsertionRole

The API SHALL support `function classifyRole(fanIn: number, fanOut: number, isHub: boolean, isEntryPoint: boolean): InsertionRole` to classify the role of a node in the codebase based on its fan-in, fan-out, and whether it's a hub or entry point.

**Request:**

```json
{
  "fanIn": "number",
  "fanOut": "number",
  "isHub": "boolean",
  "isEntryPoint": "boolean"
}
```

**Response:**

```json
{
  "role": "InsertionRole"
}
```

#### Scenario: ClassifyANodeAsAHub
- **GIVEN** A node with high fan-in and fan-out, and isHub set to true
- **WHEN** classifyRole is called
- **THEN** Returns the role as a hub

#### Scenario: ClassifyANodeAsAnEntryPoint
- **GIVEN** A node with low fan-in and high fan-out, and isEntryPoint set to true
- **WHEN** classifyRole is called
- **THEN** Returns the role as an entry point

### Requirement: FunctionInsertionRole

The API SHALL support `function compositeScore(semanticRelevance: number, role: InsertionRole): number` to calculate the composite score based on the semantic relevance and the role of the node.

**Request:**

```json
{
  "semanticRelevance": "number",
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "score": "number"
}
```

#### Scenario: CalculateCompositeScoreForAHub
- **GIVEN** A node with a role classified as a hub and a semantic relevance score
- **WHEN** compositeScore is called
- **THEN** Returns the composite score

#### Scenario: CalculateCompositeScoreForAnEntryPoint
- **GIVEN** A node with a role classified as an entry point and a semantic relevance score
- **WHEN** compositeScore is called
- **THEN** Returns the composite score

### Requirement: FunctionInsertionStrategy

The API SHALL support `function deriveStrategy(role: InsertionRole): InsertionStrategy` to derive the insertion strategy based on the classified role of a node.

**Request:**

```json
{
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "strategy": "InsertionStrategy"
}
```

#### Scenario: DeriveStrategyForAHub
- **GIVEN** A role classified as a hub
- **WHEN** deriveStrategy is called
- **THEN** Returns the strategy for a hub

#### Scenario: DeriveStrategyForAnEntryPoint
- **GIVEN** A role classified as an entry point
- **WHEN** deriveStrategy is called
- **THEN** Returns the strategy for an entry point

### Requirement: FunctionInsertionStrategy

The API SHALL support `function buildReason(name: string, role: InsertionRole, strategy: InsertionStrategy, fanIn: number, fanOut: number): unknown` to build a reason for the insertion strategy based on the node's name, role, strategy, fan-in, and fan-out.

**Request:**

```json
{
  "name": "string",
  "role": "InsertionRole",
  "strategy": "InsertionStrategy",
  "fanIn": "number",
  "fanOut": "number"
}
```

**Response:**

```json
{
  "reason": "unknown"
}
```

#### Scenario: BuildReasonForAHub
- **GIVEN** A node with a role classified as a hub and a derived strategy
- **WHEN** buildReason is called
- **THEN** Returns the reason for the insertion strategy

#### Scenario: BuildReasonForAnEntryPoint
- **GIVEN** A node with a role classified as an entry point and a derived strategy
- **WHEN** buildReason is called
- **THEN** Returns the reason for the insertion strategy

### Requirement: FunctionCodeSearch

The API SHALL support `function handleSearchCode(directory: string, query: string, limit: number = 10, language?: string, minFanIn?: number): unknown` to perform a semantic search over the codebase and enrich the results with graph neighborhood information.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "language": "string",
  "minFanIn": "number"
}
```

**Response:**

```json
{
  "results": "unknown"
}
```

#### Scenario: SearchCodeWithAQuery
- **GIVEN** A directory and a query
- **WHEN** handleSearchCode is called
- **THEN** Returns the search results enriched with graph neighborhood information

#### Scenario: SearchCodeWithAQueryAndLanguageFilter
- **GIVEN** A directory, a query, and a language filter
- **WHEN** handleSearchCode is called
- **THEN** Returns the search results filtered by language and enriched with graph neighborhood information

### Requirement: POSTCodeSearch

The API SHALL support `POST /api/mcp/search-code` to [partial spec — file too large to fully analyze (4 parts)] search for code in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "query": "string"
}
```

**Response:**

```json
{
  "results": "CodeResult[]"
}
```

#### Scenario: SearchCode
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/search-code is called
- **THEN** 200 OK with search results in response body

### Requirement: FunctionInsertionPoint

The API SHALL support `function handleSuggestInsertionPoints(directory: string, description: string, limit: number = 5, language?: string): unknown` to find the best places in the codebase to implement a new feature based on a description.

**Request:**

```json
{
  "directory": "string",
  "description": "string",
  "limit": "number",
  "language": "string"
}
```

**Response:**

```json
{
  "insertionPoints": "unknown"
}
```

#### Scenario: SuggestInsertionPointsForANewFeature
- **GIVEN** A directory and a description of the new feature
- **WHEN** handleSuggestInsertionPoints is called
- **THEN** Returns the suggested insertion points

#### Scenario: SuggestInsertionPointsForANewFeatureWithALanguageFilter
- **GIVEN** A directory, a description of the new feature, and a language filter
- **WHEN** handleSuggestInsertionPoints is called
- **THEN** Returns the suggested insertion points filtered by language

### Requirement: POSTInsertionPoint

The API SHALL support `POST /api/mcp/suggest-insertion-points` to [partial spec — file too large to fully analyze (4 parts)] suggest insertion points for new code in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "query": "string"
}
```

**Response:**

```json
{
  "points": "InsertionPoint[]"
}
```

#### Scenario: SuggestInsertionPoints
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/suggest-insertion-points is called
- **THEN** 200 OK with insertion points data in response body

### Requirement: MCPInsertionPoint

The API SHALL support `MCP suggestInsertionPoints` to [partial spec — file too large to fully analyze (4 parts)] suggest insertion points for a given code snippet.

**Request:**

```json
{
  "codeSnippet": "string"
}
```

**Response:**

```json
{
  "insertionPoints": "array"
}
```

#### Scenario: SuggestInsertionPoints
- **GIVEN** MCP server is running
- **WHEN** suggestInsertionPoints is called with the code snippet
- **THEN** Returns a list of suggested insertion points

### Requirement: FunctionSpec

The API SHALL support `function handleGetSpec(directory: string, domain: string): unknown` to return the full content of a spec domain's spec.md plus its mapping entries.

**Request:**

```json
{
  "directory": "string",
  "domain": "string"
}
```

**Response:**

```json
{
  "spec": "unknown"
}
```

#### Scenario: GetSpecForADomain
- **GIVEN** A directory and a domain
- **WHEN** handleGetSpec is called
- **THEN** Returns the full content of the spec.md and its mapping entries

### Requirement: FunctionSpecDomain

The API SHALL support `function handleListSpecDomains(directory: string): Promise<unknown>` to list all spec domains available in the project (reads openspec/specs/ directory).

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "domains": "unknown"
}
```

#### Scenario: ListSpecDomains
- **GIVEN** A directory
- **WHEN** handleListSpecDomains is called
- **THEN** Returns a list of all spec domains available in the project

### Requirement: FunctionSpecSearch

The API SHALL support `function handleSearchSpecs(directory: string, query: string, limit: number = 10, domain?: string, section?: string): unknown` to perform a semantic search over the spec index built by 'spec-gen analyze --embed'.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "domain": "string",
  "section": "string"
}
```

**Response:**

```json
{
  "results": "unknown"
}
```

#### Scenario: SearchSpecsWithAQuery
- **GIVEN** A directory and a query
- **WHEN** handleSearchSpecs is called
- **THEN** Returns the search results

#### Scenario: SearchSpecsWithAQueryAndDomainFilter
- **GIVEN** A directory, a query, and a domain filter
- **WHEN** handleSearchSpecs is called
- **THEN** Returns the search results filtered by domain

#### Scenario: SearchSpecsWithAQueryAndSectionFilter
- **GIVEN** A directory, a query, and a section filter
- **WHEN** handleSearchSpecs is called
- **THEN** Returns the search results filtered by section

### Requirement: POSTSpecSearch

The API SHALL support `POST /api/mcp/search-specs` to [partial spec — file too large to fully analyze (4 parts)] search for specifications in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "query": "string"
}
```

**Response:**

```json
{
  "results": "SpecResult[]"
}
```

#### Scenario: SearchSpecifications
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/search-specs is called
- **THEN** 200 OK with search results in response body

### Requirement: FunctionGitRepository

The API SHALL support `Function detectGitRepository(rootPath: string)` to detect if the directory is a git repository

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "hasGit": "boolean"
}
```

#### Scenario: DetectGitRepository
- **GIVEN** A root path to a directory
- **WHEN** The function is called with the root path
- **THEN** Returns true if the directory is a git repository, false otherwise

### Requirement: FunctionProjectType

The API SHALL support `Function detectProjectType(rootPath: string)` to detect the project type based on manifest files

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "projectType": "ProjectType",
  "manifestFile": "string | null",
  "hasGit": "boolean",
  "confidence": "'high' | 'medium' | 'low'"
}
```

#### Scenario: DetectProjectType
- **GIVEN** A root path to a directory
- **WHEN** The function is called with the root path
- **THEN** Returns the detected project type, manifest file, git status, and confidence level

### Requirement: FunctionProjectType

The API SHALL support `Function getProjectTypeName(type: ProjectType)` to get a human-readable project type name

**Request:**

```json
{
  "type": "ProjectType"
}
```

**Response:**

```json
{
  "name": "string"
}
```

#### Scenario: GetProjectTypeName
- **GIVEN** A project type
- **WHEN** The function is called with the project type
- **THEN** Returns the human-readable name of the project type

### Requirement: FunctionPackageJson

The API SHALL support `Function readPackageJson(rootPath: string)` to read and parse package.json if it exists

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "packageJson": "Record<string, unknown> | null"
}
```

#### Scenario: ReadPackage.json
- **GIVEN** A root path to a directory
- **WHEN** The function is called with the root path
- **THEN** Returns the parsed package.json content if it exists, null otherwise

### Requirement: ClassMcpWatcher

The API SHALL support `Class McpWatcher` to incremental re-indexer for the mcp server's --watch mode. watches source files for changes and incrementally updates signatures in llm-context.json and vector index.

#### Scenario: StartWatchingFiles
- **GIVEN** A valid project root path
- **WHEN** McpWatcher.start() is called
- **THEN** The watcher starts monitoring the project directory for changes

#### Scenario: StopWatchingFiles
- **GIVEN** The watcher is running
- **WHEN** McpWatcher.stop() is called
- **THEN** The watcher stops monitoring the project directory

#### Scenario: HandleFileChange
- **GIVEN** A source file is changed
- **WHEN** The watcher detects the change
- **THEN** The watcher re-indexes the file and updates the llm-context.json and vector index

### Requirement: FunctionGenerate

The API SHALL support `Function specGenGenerate(options: GenerateApiOptions = {})` to generate openspec specification files from analysis results using llm.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "GenerateApiOptions": {
        "type": "object"
      }
    }
  }
}
```

**Response:**

```json
{
  "GenerateResult": {
    "type": "object"
  }
}
```

#### Scenario: GenerateOpenSpecFiles
- **GIVEN** Analysis results available
- **WHEN** specGenGenerate is called with options
- **THEN** OpenSpec specification files are generated and returned as GenerateResult

### Requirement: GETSpecRequirement

The API SHALL support `GET /api/specs/requirements` to read spec requirements referenced in mapping.json.

**Request:**

```json
{
  "rootPath": "string (optional)"
}
```

**Response:**

```json
{
  "generatedAt": "string (optional)",
  "requirements": {
    "type": "object",
    "properties": {
      "requirementKey": {
        "type": "object",
        "properties": {
          "title": "string",
          "body": "string",
          "specFile": "string (optional)",
          "domain": "string (optional)",
          "service": "string (optional)"
        }
      }
    }
  }
}
```

#### Scenario: GetSpecRequirements
- **GIVEN** A valid mapping.json file exists in the project root
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with the requirements object in the response body
- **AND** The response includes the generatedAt timestamp if available
- **AND** Each requirement includes the title, body, specFile, domain, and service if available

#### Scenario: GetSpecRequirementsWithMissingMapping.json
- **GIVEN** No mapping.json file exists in the project root
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with an empty requirements object in the response body

#### Scenario: GetSpecRequirementsWithInvalidMapping.json
- **GIVEN** An invalid mapping.json file exists in the project root
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with an empty requirements object in the response body

### Requirement: POSTSpecGen

The API SHALL support `POST /api/run` to run the full spec-gen pipeline: init → analyze → generate.

**Request:**

```json
{
  "options": "RunApiOptions"
}
```

**Response:**

```json
{
  "result": "RunResult"
}
```

#### Scenario: RunSpecGenPipeline
- **GIVEN** Valid RunApiOptions
- **WHEN** POST /api/run is called
- **THEN** 200 OK with RunResult in response body

### Requirement: FunctionProjectInitialization

The API SHALL support `Function specGenInit(options: InitApiOptions = {})` to initialize spec-gen in a project directory.

**Request:**

```json
{
  "options": "InitApiOptions"
}
```

**Response:**

```json
{
  "result": "InitResult"
}
```

#### Scenario: InitializeSpecGen
- **GIVEN** A project directory
- **WHEN** specGenInit is called with options
- **THEN** spec-gen is initialized in the project directory
- **AND** Creates necessary configuration files
- **AND** Sets up the OpenSpec structure
- **AND** Updates .gitignore if needed

### Requirement: FunctionSpecVerification

The API SHALL support `Function specGenVerify(options: VerifyApiOptions = {})` to verify generated specs against actual source code by sampling files and validating that specs accurately describe behavior using an llm to predict behavior from specs and compare against code.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "samples": "number",
    "threshold": "number",
    "onProgress": "ProgressCallback",
    "provider": "string",
    "model": "string",
    "apiBase": "string",
    "sslVerify": "boolean",
    "openaiCompatBaseUrl": "string"
  }
}
```

**Response:**

```json
{
  "report": "VerifyResult",
  "duration": "number"
}
```

#### Scenario: SuccessfulVerification
- **GIVEN** Valid spec-gen configuration, existing specs, analysis, and LLM API key
- **WHEN** specGenVerify() is called with valid options
- **THEN** Returns a VerifyResult object with verification report and duration
- **AND** Progress callbacks are called with verification steps
- **AND** LLM logs are saved

#### Scenario: MissingConfiguration
- **GIVEN** No spec-gen configuration
- **WHEN** specGenVerify() is called
- **THEN** Throws an error indicating no configuration was found

#### Scenario: MissingSpecs
- **GIVEN** No existing specs
- **WHEN** specGenVerify() is called
- **THEN** Throws an error indicating no specs were found

#### Scenario: MissingAnalysis
- **GIVEN** No existing analysis
- **WHEN** specGenVerify() is called
- **THEN** Throws an error indicating no analysis was found

#### Scenario: MissingLLMAPIKey
- **GIVEN** No LLM API key
- **WHEN** specGenVerify() is called
- **THEN** Throws an error indicating no LLM API key was found

#### Scenario: NoVerificationCandidates
- **GIVEN** No suitable verification candidates
- **WHEN** specGenVerify() is called
- **THEN** Throws an error indicating no verification candidates were found

### Requirement: POSTSpecVerification

The API SHALL support `POST /api/verify` to verify generated specs against actual source code.

**Request:**

```json
{
  "rootPath": "string",
  "samples": "number",
  "threshold": "number",
  "provider": "string",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string"
}
```

**Response:**

```json
{
  "report": "object",
  "duration": "number"
}
```

#### Scenario: SuccessfulVerification
- **GIVEN** Valid spec-gen configuration, existing specs, and LLM API key
- **WHEN** POST /api/verify is called with valid parameters
- **THEN** 200 OK with verification report and duration in response body
- **AND** Verification report includes overall confidence score
- **AND** LLM logs are saved

#### Scenario: MissingConfiguration
- **GIVEN** No spec-gen configuration exists
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

#### Scenario: MissingLLMAPIKey
- **GIVEN** No LLM API key is set in environment variables
- **WHEN** POST /api/verify is called
- **THEN** 400 Bad Request with error message

### Requirement: POSTSpecification

The API SHALL support `POST /api/drift/specGenDrift` to detect spec drift in a project by analyzing changes and identifying gaps, stale specs, and uncovered files.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "baseRef": {
        "type": "string",
        "description": "The base Git reference to compare against"
      },
      "threshold": {
        "type": "number",
        "description": "The minimum severity threshold for reporting drift"
      },
      "llmService": {
        "type": "string",
        "description": "The LLM service to use for enhanced analysis"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "gaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string",
            "description": "The file path"
          },
          "severity": {
            "type": "number",
            "description": "The severity of the gap"
          },
          "reason": {
            "type": "string",
            "description": "The reason for the gap"
          }
        }
      }
    },
    "staleSpecs": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "List of stale specification files"
      }
    },
    "uncoveredFiles": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "List of files not covered by specifications"
      }
    },
    "orphanedSpecs": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "List of specifications that are no longer relevant"
      }
    },
    "adrGaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "adrId": {
            "type": "string",
            "description": "The ADR ID"
          },
          "severity": {
            "type": "number",
            "description": "The severity of the gap"
          },
          "reason": {
            "type": "string",
            "description": "The reason for the gap"
          }
        }
      }
    },
    "adrOrphaned": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "List of ADRs that are no longer relevant"
      }
    }
  }
}
```

#### Scenario: DetectSpecDrift
- **GIVEN** A project with specifications and code changes
- **WHEN** POST /api/drift/specGenDrift is called with baseRef and threshold
- **THEN** 200 OK with drift analysis results including gaps, stale specs, uncovered files, orphaned specs, ADR
gaps, and ADR orphaned
- **AND** The response includes enhanced analysis using LLM if configured
- **AND** The response includes severity scores for each issue

#### Scenario: DetectSpecDriftWithLLM
- **GIVEN** A project with specifications and code changes and an LLM service configured
- **WHEN** POST /api/drift/specGenDrift is called with baseRef, threshold, and llmService
- **THEN** 200 OK with drift analysis results including enhanced analysis using LLM
- **AND** The response includes more detailed reasons for each issue
- **AND** The response includes severity scores for each issue

### Requirement: CLICommandSpecification

The API SHALL support `CLI Command spec-gen verify` to [partial spec — file too large to fully analyze (2 parts)] verify generated specs against actual source code to ensure accuracy.

**Request:**

```json
{
  "samples": "number",
  "threshold": "number",
  "files": "string[]",
  "domains": "string[]",
  "verbose": "boolean",
  "json": "boolean"
}
```

**Response:**

```json
{
  "results": "VerificationResult[]",
  "confidenceScore": "number",
  "recommendation": "string",
  "domains": "DomainVerificationResult[]"
}
```

#### Scenario: BasicVerification
- **GIVEN** Generated specs exist and LLM API key is configured
- **WHEN** spec-gen verify is run with default options
- **THEN** Verification report is generated with confidence score and recommendations

#### Scenario: SpecificFilesVerification
- **GIVEN** Specific files to verify are provided
- **WHEN** spec-gen verify --files file1,file2 is run
- **THEN** Only the specified files are verified

#### Scenario: JSONOutput
- **GIVEN** JSON output is requested
- **WHEN** spec-gen verify --json is run
- **THEN** Verification results are output as JSON

### Requirement: CommandSpecification

The API SHALL support `Command drift` to [partial spec — file too large to fully analyze (2 parts)] detects and reports on specification drift between code and specifications.

**Request:**

```json
{
  "maxFiles": "number",
  "failOn": "string",
  "json": "boolean",
  "verbose": "boolean",
  "installHook": "boolean",
  "uninstallHook": "boolean"
}
```

**Response:**

```json
{
  "issues": "DriftIssue[]",
  "summary": {
    "gaps": "number",
    "stale": "number",
    "uncovered": "number",
    "orphanedSpecs": "number",
    "adrGaps": "number",
    "adrOrphaned": "number"
  }
}
```

#### Scenario: DetectDriftInCodebase
- **GIVEN** A codebase with specifications
- **WHEN** The drift command is executed
- **THEN** A list of drift issues is returned
- **AND** The issues are categorized by severity
- **AND** A summary of the issues is provided

#### Scenario: InstallPreCommitHook
- **GIVEN** A git repository
- **WHEN** The install-hook option is used
- **THEN** A pre-commit hook is installed
- **AND** The hook checks for drift before each commit

#### Scenario: UninstallPreCommitHook
- **GIVEN** An installed pre-commit hook
- **WHEN** The uninstall-hook option is used
- **THEN** The pre-commit hook is removed

### Requirement: GETSpecification

The API SHALL support `GET /api/spec` to [partial spec — file too large to fully analyze (2 parts)] retrieve the combined specification files.

**Response:**

```json
{
  "spec": "string"
}
```

#### Scenario: GetSpecFiles
- **GIVEN** Spec directory exists
- **WHEN** GET /api/spec is called
- **THEN** 200 OK with combined spec content in response body

### Requirement: MCPSpecification

The API SHALL support `MCP searchSpecs` to [partial spec — file too large to fully analyze (4 parts)] search for specifications in the codebase.

**Request:**

```json
{
  "query": "string"
}
```

**Response:**

```json
{
  "results": "array"
}
```

#### Scenario: SearchSpecifications
- **GIVEN** MCP server is running
- **WHEN** searchSpecs is called with the query
- **THEN** Returns a list of specification search results

### Requirement: MCPSpecification

The API SHALL support `MCP getSpec` to [partial spec — file too large to fully analyze (4 parts)] get a specification by its id.

**Request:**

```json
{
  "specId": "string"
}
```

**Response:**

```json
{
  "specification": "object"
}
```

#### Scenario: GetSpecification
- **GIVEN** MCP server is running and the specification exists
- **WHEN** getSpec is called with the specification ID
- **THEN** Returns the specification details

### Requirement: CLICommandCodebaseAnalysis

The API SHALL support `CLI Command spec-gen analyze` to [partial spec — file too large to fully analyze (3 parts)] run static analysis on the codebase without llm involvement. outputs repository map, dependency graph, and file significance scores.

**Request:**

```json
{
  "output": "string",
  "maxFiles": "number",
  "include": "string[]",
  "exclude": "string[]",
  "force": "boolean",
  "embed": "boolean",
  "reindexSpecs": "boolean",
  "quiet": "boolean",
  "verbose": "boolean",
  "noColor": "boolean",
  "config": "string"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "dependencyGraph": "object",
  "llmContext": "object",
  "artifacts": "object",
  "outputFiles": "string[]"
}
```

#### Scenario: AnalyzeWithDefaults
- **GIVEN** A codebase with spec-gen configuration
- **WHEN** The command 'spec-gen analyze' is executed
- **THEN** The codebase is analyzed and results are written to the default output directory
- **AND** Repository structure is generated
- **AND** Dependency graph is created
- **AND** File significance scores are calculated

#### Scenario: AnalyzeWithCustomOptions
- **GIVEN** A codebase with spec-gen configuration
- **WHEN** The command 'spec-gen analyze --max-files 1000 --include "*.graphql" --exclude "legacy/**"' is
executed
- **THEN** The codebase is analyzed with the specified options and results are written to the default output
directory
- **AND** 1000 files are analyzed
- **AND** GraphQL files are included
- **AND** Legacy files are excluded

#### Scenario: ForceReAnalysis
- **GIVEN** A codebase with existing analysis results
- **WHEN** The command 'spec-gen analyze --force' is executed
- **THEN** The codebase is re-analyzed and new results are written to the default output directory
- **AND** Existing analysis results are overwritten

#### Scenario: ReIndexSpecsOnly
- **GIVEN** A codebase with existing analysis results and OpenSpec specs
- **WHEN** The command 'spec-gen analyze --reindex-specs' is executed
- **THEN** The OpenSpec specs are re-indexed into the vector index without re-running full analysis
- **AND** Full analysis is skipped
- **AND** Specs are re-indexed

### Requirement: FunctionVectorIndex

The API SHALL support `Function runEmbedStep(rootPath: string, outputPath: string, specGenConfig: SpecGenConfig | null, force: boolean, llmContext: import('../../core/analyzer/artifact-generator.js').LLMContext | null): Promise<void>` to [partial spec — file too large to fully analyze (3 parts)] builds a semantic vector index for the codebase without involving llm.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "specGenConfig": "SpecGenConfig | null",
  "force": "boolean",
  "llmContext": "LLMContext | null"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "details": "object"
}
```

#### Scenario: BuildVectorIndexSuccessfully
- **GIVEN** Valid rootPath, outputPath, and specGenConfig
- **WHEN** runEmbedStep is called
- **THEN** Vector index is built and saved to outputPath
- **AND** Function index is built with call graph data
- **AND** Spec index is built if specs directory exists

#### Scenario: FailToBuildVectorIndex
- **GIVEN** Invalid rootPath or outputPath
- **WHEN** runEmbedStep is called
- **THEN** Error is logged and function exits gracefully

### Requirement: FunctionSpecVectorIndex

The API SHALL support `Function runSpecIndexing(rootPath: string, outputPath: string, specGenConfig: SpecGenConfig | null): Promise<void>` to [partial spec — file too large to fully analyze (3 parts)] indexes openspec specs into the vector index.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "specGenConfig": "SpecGenConfig | null"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "details": "object"
}
```

#### Scenario: IndexSpecsSuccessfully
- **GIVEN** Valid rootPath, outputPath, and specGenConfig
- **WHEN** runSpecIndexing is called
- **THEN** Specs are indexed and saved to outputPath
- **AND** Specs directory exists and contains valid specs

#### Scenario: SkipSpecIndexing
- **GIVEN** Specs directory does not exist or embedding config is missing
- **WHEN** runSpecIndexing is called
- **THEN** Function exits gracefully without error

### Requirement: CommandSystemHealth

The API SHALL support `Command doctor` to [partial spec — file too large to fully analyze (2 parts)] run system health checks to verify the environment is properly configured for spec-gen operations.

#### Scenario: RunDoctorCommand
- **GIVEN** User has executed the doctor command
- **WHEN** The command is run
- **THEN** The system performs various health checks and displays the results

### Requirement: CLICommandSystemDiagnostics

The API SHALL support `CLI Command spec-gen doctor` to [partial spec — file too large to fully analyze (2 parts)] check your environment and configuration for common issues

#### Scenario: RunAllChecks
- **GIVEN** User runs the doctor command
- **WHEN** spec-gen doctor is executed
- **THEN** All checks are performed and results are displayed

#### Scenario: OutputResultsAsJSON
- **GIVEN** User runs the doctor command with --json flag
- **WHEN** spec-gen doctor --json is executed
- **THEN** Results are output as JSON

### Requirement: POSTCodebase

The API SHALL support `POST /api/mcp/orient` to analyze a codebase and provide recommendations for refactoring based on architectural orientation.

**Request:**

```json
{
  "directory": "string",
  "config": "object"
}
```

**Response:**

```json
{
  "recommendations": "array",
  "score": "number",
  "reason": "string"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** A valid directory path and configuration
- **WHEN** POST /api/mcp/orient is called
- **THEN** 200 OK with refactoring recommendations and score

### Requirement: POSTCodebase

The API SHALL support `POST /api/mcp/analyze` to analyze a codebase for refactoring opportunities and generate a detailed report.

**Request:**

```json
{
  "directory": "string",
  "config": "object"
}
```

**Response:**

```json
{
  "report": "object",
  "analysis": "object"
}
```

#### Scenario: GenerateRefactoringReport
- **GIVEN** A valid directory path and configuration
- **WHEN** POST /api/mcp/analyze is called
- **THEN** 200 OK with detailed refactoring report

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/architecture` to generate an overview of the architecture of a codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "overview": "object"
}
```

#### Scenario: GetArchitectureOverview
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/architecture is called
- **THEN** 200 OK with architecture overview

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/refactor-report` to generate a detailed report on refactoring opportunities in a codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "report": "object"
}
```

#### Scenario: GetRefactoringReport
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/refactor-report is called
- **THEN** 200 OK with refactoring report

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/call-graph` to generate a call graph for a codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "graph": "object"
}
```

#### Scenario: GetCallGraph
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/call-graph is called
- **THEN** 200 OK with call graph

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/signatures` to retrieve function signatures for a codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "signatures": "array"
}
```

#### Scenario: GetFunctionSignatures
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/signatures is called
- **THEN** 200 OK with function signatures

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/subgraph` to generate a subgraph for a specific function in a codebase.

**Request:**

```json
{
  "directory": "string",
  "functionName": "string"
}
```

**Response:**

```json
{
  "subgraph": "object"
}
```

#### Scenario: GetSubgraph
- **GIVEN** A valid directory path and function name
- **WHEN** GET /api/mcp/subgraph is called
- **THEN** 200 OK with subgraph

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/trace` to trace the execution path of a function in a codebase.

**Request:**

```json
{
  "directory": "string",
  "functionName": "string"
}
```

**Response:**

```json
{
  "trace": "array"
}
```

#### Scenario: TraceExecutionPath
- **GIVEN** A valid directory path and function name
- **WHEN** GET /api/mcp/trace is called
- **THEN** 200 OK with execution trace

### Requirement: GETCodebase

The API SHALL support `GET /api/mcp/mapping` to retrieve the mapping of functions to specifications in a codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetFunctionMapping
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/mapping is called
- **THEN** 200 OK with function mapping

### Requirement: POSTCodebase

The API SHALL support `POST /api/mcp/impact` to analyze the impact of a change in a codebase.

**Request:**

```json
{
  "directory": "string",
  "change": "object"
}
```

**Response:**

```json
{
  "impact": "object"
}
```

#### Scenario: AnalyzeImpact
- **GIVEN** A valid directory path and change description
- **WHEN** POST /api/mcp/impact is called
- **THEN** 200 OK with impact analysis

### Requirement: FunctionCodebase

The API SHALL support `Function specGenAnalyze(options)` to run static analysis on the codebase. scans the repository, builds a dependency graph, and generates analysis artifacts. no llm involvement.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "maxFiles": "number",
    "excludePatterns": "string[]",
    "includePatterns": "string[]",
    "force": "boolean",
    "outputPath": "string",
    "onProgress": "function"
  }
}
```

**Response:**

```json
{
  "repoMap": "object",
  "depGraph": "object",
  "artifacts": "object",
  "duration": "number"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** A codebase with spec-gen configuration
- **WHEN** specGenAnalyze() is called
- **THEN** Returns repository map, dependency graph, artifacts, and duration
- **AND** Scans the directory structure
- **AND** Builds a dependency graph
- **AND** Generates analysis artifacts

#### Scenario: SkipAnalysis
- **GIVEN** Recent analysis exists and force is false
- **WHEN** specGenAnalyze() is called
- **THEN** Returns cached repository map, dependency graph, artifacts, and duration

#### Scenario: ErrorOnMissingConfig
- **GIVEN** No spec-gen configuration found
- **WHEN** specGenAnalyze() is called
- **THEN** Throws an error

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/orient` to [partial spec — file too large to fully analyze (4 parts)] get a comprehensive overview of relevant functions, source files, spec domains, and insertion points for a given task.

**Request:**

```json
{
  "directory": "string",
  "task": "string",
  "limit": "number"
}
```

**Response:**

```json
{
  "functions": "array",
  "files": "array",
  "specDomains": "array",
  "insertionPoints": "array",
  "specSections": "array"
}
```

#### Scenario: GetTaskOverview
- **GIVEN** A project directory and a task description
- **WHEN** POST /mcp/orient is called
- **THEN** 200 OK with relevant functions, files, spec domains, insertion points, and spec sections

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/analyze_codebase` to [partial spec — file too large to fully analyze (4 parts)] analyze a codebase to build the call graph, dependency graph, and refactor priorities.

**Request:**

```json
{
  "directory": "string",
  "force": "boolean"
}
```

**Response:**

```json
{
  "status": "string"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** A project directory
- **WHEN** POST /mcp/analyze_codebase is called
- **THEN** 200 OK with analysis status

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_architecture_overview` to [partial spec — file too large to fully analyze (4 parts)] get an overview of the architecture of a codebase, including domain clusters, cross-cluster dependencies, global entry points, and critical hubs.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "domainClusters": "array",
  "crossClusterDependencies": "array",
  "globalEntryPoints": "array",
  "criticalHubs": "array"
}
```

#### Scenario: GetArchitectureOverview
- **GIVEN** A project directory
- **WHEN** POST /mcp/get_architecture_overview is called
- **THEN** 200 OK with domain clusters, cross-cluster dependencies, global entry points, and critical hubs

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_refactor_report` to [partial spec — file too large to fully analyze (4 parts)] get a prioritized list of functions that need refactoring based on static analysis.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "functions": "array"
}
```

#### Scenario: GetRefactorReport
- **GIVEN** A project directory
- **WHEN** POST /mcp/get_refactor_report is called
- **THEN** 200 OK with a list of functions that need refactoring

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_call_graph` to [partial spec — file too large to fully analyze (4 parts)] get the call graph for a project, including hub functions, entry points, and architectural layer violations.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "hubFunctions": "array",
  "entryPoints": "array",
  "architecturalLayerViolations": "array"
}
```

#### Scenario: GetCallGraph
- **GIVEN** A project directory
- **WHEN** POST /mcp/get_call_graph is called
- **THEN** 200 OK with hub functions, entry points, and architectural layer violations

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_duplicate_report` to [partial spec — file too large to fully analyze (4 parts)] detect duplicate code (clone groups) across the codebase using pure static analysis.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "cloneGroups": "array"
}
```

#### Scenario: GetDuplicateReport
- **GIVEN** A project directory
- **WHEN** POST /mcp/get_duplicate_report is called
- **THEN** 200 OK with clone groups

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_signatures` to [partial spec — file too large to fully analyze (4 parts)] get compact function and class signatures for files in a project.

**Request:**

```json
{
  "directory": "string",
  "filePattern": "string"
}
```

**Response:**

```json
{
  "signatures": "array"
}
```

#### Scenario: GetSignatures
- **GIVEN** A project directory and an optional file pattern
- **WHEN** POST /mcp/get_signatures is called
- **THEN** 200 OK with function and class signatures

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_subgraph` to [partial spec — file too large to fully analyze (4 parts)] extract a subgraph of the call graph centered on a specific function.

**Request:**

```json
{
  "directory": "string",
  "functionName": "string",
  "direction": "string",
  "maxDepth": "number",
  "format": "string"
}
```

**Response:**

```json
{
  "subgraph": "object"
}
```

#### Scenario: GetSubgraph
- **GIVEN** A project directory, function name, direction, max depth, and format
- **WHEN** POST /mcp/get_subgraph is called
- **THEN** 200 OK with the subgraph

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/trace_execution_path` to [partial spec — file too large to fully analyze (4 parts)] find all execution paths between two functions in the call graph.

**Request:**

```json
{
  "directory": "string",
  "entryFunction": "string",
  "targetFunction": "string",
  "maxDepth": "number",
  "maxPaths": "number"
}
```

**Response:**

```json
{
  "paths": "array"
}
```

#### Scenario: TraceExecutionPath
- **GIVEN** A project directory, entry function, target function, max depth, and max paths
- **WHEN** POST /mcp/trace_execution_path is called
- **THEN** 200 OK with execution paths

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_mapping` to [partial spec — file too large to fully analyze (4 parts)] get the requirement to function mapping produced by spec-gen generate.

**Request:**

```json
{
  "directory": "string",
  "domain": "string",
  "orphansOnly": "boolean"
}
```

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetMapping
- **GIVEN** A project directory, domain, and orphansOnly flag
- **WHEN** POST /mcp/get_mapping is called
- **THEN** 200 OK with the requirement to function mapping

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/check_spec_drift` to [partial spec — file too large to fully analyze (4 parts)] check for drift between the code and the specifications.

**Request:**

```json
{
  "directory": "string",
  "base": "string",
  "files": "array",
  "domains": "array",
  "failOn": "string",
  "maxFiles": "number"
}
```

**Response:**

```json
{
  "drift": "array"
}
```

#### Scenario: CheckSpecDrift
- **GIVEN** A project directory, base, files, domains, failOn, and maxFiles
- **WHEN** POST /mcp/check_spec_drift is called
- **THEN** 200 OK with drift information

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/analyze_impact` to [partial spec — file too large to fully analyze (4 parts)] analyze the impact of modifying a function.

**Request:**

```json
{
  "directory": "string",
  "symbol": "string",
  "depth": "number"
}
```

**Response:**

```json
{
  "impact": "object"
}
```

#### Scenario: AnalyzeImpact
- **GIVEN** A project directory, symbol, and depth
- **WHEN** POST /mcp/analyze_impact is called
- **THEN** 200 OK with impact analysis

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_low_risk_refactor_candidates` to [partial spec — file too large to fully analyze (4 parts)] get the safest functions to refactor first.

**Request:**

```json
{
  "directory": "string",
  "limit": "number",
  "filePattern": "string"
}
```

**Response:**

```json
{
  "candidates": "array"
}
```

#### Scenario: GetLowRiskRefactorCandidates
- **GIVEN** A project directory, limit, and file pattern
- **WHEN** POST /mcp/get_low_risk_refactor_candidates is called
- **THEN** 200 OK with low risk refactor candidates

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_leaf_functions` to [partial spec — file too large to fully analyze (4 parts)] get functions that make no internal calls (leaves of the call graph).

**Request:**

```json
{
  "directory": "string",
  "limit": "number",
  "filePattern": "string",
  "sortBy": "string"
}
```

**Response:**

```json
{
  "functions": "array"
}
```

#### Scenario: GetLeafFunctions
- **GIVEN** A project directory, limit, file pattern, and sortBy
- **WHEN** POST /mcp/get_leaf_functions is called
- **THEN** 200 OK with leaf functions

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_critical_hubs` to [partial spec — file too large to fully analyze (4 parts)] get the highest-impact hub functions.

**Request:**

```json
{
  "directory": "string",
  "limit": "number",
  "minFanIn": "number"
}
```

**Response:**

```json
{
  "hubs": "array"
}
```

#### Scenario: GetCriticalHubs
- **GIVEN** A project directory, limit, and minFanIn
- **WHEN** POST /mcp/get_critical_hubs is called
- **THEN** 200 OK with critical hubs

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_function_skeleton` to [partial spec — file too large to fully analyze (4 parts)] get a noise-stripped skeleton of a source file.

**Request:**

```json
{
  "directory": "string",
  "filePath": "string"
}
```

**Response:**

```json
{
  "skeleton": "string"
}
```

#### Scenario: GetFunctionSkeleton
- **GIVEN** A project directory and file path
- **WHEN** POST /mcp/get_function_skeleton is called
- **THEN** 200 OK with the function skeleton

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_god_functions` to [partial spec — file too large to fully analyze (4 parts)] detect god functions (high fan-out, likely orchestrators) in the project or in a specific file.

**Request:**

```json
{
  "directory": "string",
  "filePath": "string",
  "fanOutThreshold": "number"
}
```

**Response:**

```json
{
  "functions": "array"
}
```

#### Scenario: GetGodFunctions
- **GIVEN** A project directory, file path, and fanOutThreshold
- **WHEN** POST /mcp/get_god_functions is called
- **THEN** 200 OK with god functions

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/suggest_insertion_points` to [partial spec — file too large to fully analyze (4 parts)] suggest insertion points for a new feature based on a natural language description.

**Request:**

```json
{
  "directory": "string",
  "description": "string",
  "limit": "number",
  "language": "string"
}
```

**Response:**

```json
{
  "insertionPoints": "array"
}
```

#### Scenario: SuggestInsertionPoints
- **GIVEN** A project directory, description, limit, and language
- **WHEN** POST /mcp/suggest_insertion_points is called
- **THEN** 200 OK with insertion points

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/search_code` to [partial spec — file too large to fully analyze (4 parts)] search for code based on a natural language query.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "language": "string",
  "minFanIn": "number"
}
```

**Response:**

```json
{
  "results": "array"
}
```

#### Scenario: SearchCode
- **GIVEN** A project directory, query, limit, language, and minFanIn
- **WHEN** POST /mcp/search_code is called
- **THEN** 200 OK with search results

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/list_spec_domains` to [partial spec — file too large to fully analyze (4 parts)] list all openspec domains available in a project.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "domains": "array"
}
```

#### Scenario: ListSpecDomains
- **GIVEN** A project directory
- **WHEN** POST /mcp/list_spec_domains is called
- **THEN** 200 OK with a list of spec domains

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/search_specs` to [partial spec — file too large to fully analyze (4 parts)] search for specifications based on a natural language query.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "domain": "string",
  "section": "string"
}
```

**Response:**

```json
{
  "results": "array"
}
```

#### Scenario: SearchSpecs
- **GIVEN** A project directory, query, limit, domain, and section
- **WHEN** POST /mcp/search_specs is called
- **THEN** 200 OK with search results

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_spec` to [partial spec — file too large to fully analyze (4 parts)] get the full content of a spec domain's specification file and the functions that implement it.

**Request:**

```json
{
  "directory": "string",
  "domain": "string"
}
```

**Response:**

```json
{
  "spec": "object",
  "functions": "array"
}
```

#### Scenario: GetSpec
- **GIVEN** A project directory and domain
- **WHEN** POST /mcp/get_spec is called
- **THEN** 200 OK with the spec and functions

### Requirement: POSTCodebase

The API SHALL support `POST /mcp/get_function_body` to [partial spec — file too large to fully analyze (4 parts)] get the exact source code of a named function in a file.

**Request:**

```json
{
  "directory": "string",
  "filePath": "string",
  "functionName": "string"
}
```

**Response:**

```json
{
  "body": "string"
}
```

#### Scenario: GetFunctionBody
- **GIVEN** A project directory, file path, and function name
- **WHEN** POST /mcp/get_function_body is called
- **THEN** 200 OK with the function body

### Requirement: FunctionAnalysisData

The API SHALL support `Function loadAnalysis(analysisPath: string)` to [partial spec — file too large to fully analyze (2 parts)] load analysis data from disk

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "repoStructure": "RepoStructure",
  "llmContext": "LLMContext",
  "depGraph": "DependencyGraphResult",
  "age": "number",
  "timestamp": "string"
}
```

#### Scenario: LoadAnalysisData
- **GIVEN** Valid analysis path
- **WHEN** loadAnalysis is called
- **THEN** Returns analysis data including repo structure, LLM context, dependency graph, age, and timestamp

### Requirement: FunctionAnalysisData

The API SHALL support `function loadAnalysisData(analysisPath)` to [partial spec — file too large to fully analyze (2 parts)] loads analysis data from the specified analysis path.

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "llmContext": "object",
  "depGraph": "object",
  "refactorReport": "object"
}
```

#### Scenario: LoadAnalysisData
- **GIVEN** A valid analysis path
- **WHEN** The function is called with the analysis path
- **THEN** The function returns an object containing repoStructure, llmContext, depGraph, and refactorReport

### Requirement: FunctionCostEstimation

The API SHALL support `Function estimateCost(llmContext: LLMContext, provider: string, model: string)` to [partial spec — file too large to fully analyze (2 parts)] estimate cost for the full generation pipeline (all stages)

**Request:**

```json
{
  "llmContext": "LLMContext",
  "provider": "string",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** Valid LLM context, provider, and model
- **WHEN** estimateCost is called
- **THEN** Returns estimated tokens and cost for the full generation pipeline

### Requirement: FunctionCostEstimation

The API SHALL support `Function estimateCost(llmContext: LLMContext, model: string)` to [partial spec — file too large to fully analyze (2 parts)] estimate the cost for generation based on the llm context and model.

**Request:**

```json
{
  "llmContext": "LLMContext",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** Valid LLM context and model
- **WHEN** Function is called
- **THEN** Returns the estimated tokens and cost

### Requirement: FunctionCostEstimation

The API SHALL support `Function estimateCost(llmContext, provider, model)` to [partial spec — file too large to fully analyze (2 parts)] estimate cost for the full generation pipeline (all stages) based on llm context, provider, and model.

**Request:**

```json
{
  "llmContext": "object",
  "provider": "string",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** Valid LLM context, provider, and model
- **WHEN** Function is called with llmContext, provider, and model
- **THEN** Returns estimated tokens and cost for the generation pipeline

### Requirement: FunctionCostEstimation

The API SHALL support `Function estimateCost(llmContext, model)` to [partial spec — file too large to fully analyze (2 parts)] estimate the cost for generation.

**Request:**

```json
{
  "llmContext": "object",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** LLM context and model
- **WHEN** The function is called
- **THEN** Returns the estimated tokens and cost

### Requirement: FunctionUserConfirmation

The API SHALL support `Function promptConfirmation(message: string, autoYes: boolean)` to [partial spec — file too large to fully analyze (2 parts)] prompt user for confirmation

**Request:**

```json
{
  "message": "string",
  "autoYes": "boolean"
}
```

**Response:**

```json
{
  "confirmation": "boolean"
}
```

#### Scenario: PromptConfirmation
- **GIVEN** Message and autoYes flag
- **WHEN** promptConfirmation is called
- **THEN** Returns user confirmation or auto-confirmation based on autoYes flag

### Requirement: FunctionAPIConnectivity

The API SHALL support `Function verifyApiConnectivity(llm: LLMService)` to [partial spec — file too large to fully analyze (2 parts)] verify llm api connectivity

**Request:**

```json
{
  "llm": "LLMService"
}
```

**Response:**

```json
{
  "success": "boolean"
}
```

#### Scenario: VerifyAPIConnectivity
- **GIVEN** Valid LLM service
- **WHEN** verifyApiConnectivity is called
- **THEN** Returns success status of API connectivity verification

### Requirement: FunctionAPIConnectivity

The API SHALL support `Function verifyApiConnectivity(llm)` to [partial spec — file too large to fully analyze (2 parts)] verify llm api connectivity by making a test call.

**Request:**

```json
{
  "llm": "object"
}
```

**Response:**

```json
{
  "success": "boolean"
}
```

#### Scenario: VerifyAPIConnectivity
- **GIVEN** LLM service instance
- **WHEN** Function is called with LLM instance
- **THEN** Returns true if API call succeeds, false otherwise

### Requirement: CLICommandSpecificationGeneration

The API SHALL support `CLI Command spec-gen generate` to [partial spec — file too large to fully analyze (2 parts)] generate openspec files from analysis using llm

**Request:**

```json
{
  "analysis": "string",
  "model": "string",
  "dryRun": "boolean",
  "domains": "string[]",
  "adr": "boolean",
  "adrOnly": "boolean",
  "reanalyze": "boolean",
  "merge": "boolean",
  "noOverwrite": "boolean",
  "yes": "boolean",
  "outputDir": "string"
}
```

**Response:**

```json
{
  "filesWritten": "string[]",
  "filesMerged": "string[]",
  "filesSkipped": "string[]",
  "filesBackedUp": "string[]",
  "configUpdated": "boolean",
  "warnings": "string[]",
  "validationErrors": "string[]",
  "nextSteps": "string[]"
}
```

#### Scenario: GenerateSpecsWithDefaultSettings
- **GIVEN** Existing analysis and API key
- **WHEN** User runs `spec-gen generate`
- **THEN** OpenSpec files are generated in the openspec/specs/ directory
- **AND** Pipeline stages are completed
- **AND** Cost estimate is displayed
- **AND** Generation report is saved

#### Scenario: DryRunGeneration
- **GIVEN** Existing analysis and API key
- **WHEN** User runs `spec-gen generate --dry-run`
- **THEN** No files are written
- **AND** Pipeline stages are shown
- **AND** Domains to generate are listed
- **AND** Output paths are displayed

#### Scenario: GenerateSpecificDomains
- **GIVEN** Existing analysis and API key
- **WHEN** User runs `spec-gen generate --domains auth,api`
- **THEN** Only auth and api specs are generated
- **AND** Other domains are skipped
- **AND** Cost estimate is adjusted
- **AND** Generation report reflects filtered domains

#### Scenario: MergeWithExistingSpecs
- **GIVEN** Existing analysis, API key, and specs
- **WHEN** User runs `spec-gen generate --merge`
- **THEN** New content is merged with existing specs
- **AND** Existing content is preserved
- **AND** New sections are added
- **AND** Backups are created

#### Scenario: SkipExistingSpecs
- **GIVEN** Existing analysis, API key, and specs
- **WHEN** User runs `spec-gen generate --no-overwrite`
- **THEN** Existing specs are skipped
- **AND** Only new specs are generated
- **AND** No backups are created
- **AND** Generation report shows skipped files

### Requirement: FunctionRunMetadata

The API SHALL support `Function saveRunMetadata(rootPath: string, metadata: RunMetadata)` to [partial spec — file too large to fully analyze (2 parts)] save run metadata to disk.

**Request:**

```json
{
  "rootPath": "string",
  "metadata": "RunMetadata"
}
```

#### Scenario: SaveRunMetadata
- **GIVEN** Valid root path and metadata
- **WHEN** Function is called
- **THEN** Saves the metadata to disk

### Requirement: FunctionRunMetadata

The API SHALL support `Function saveRunMetadata(rootPath, metadata)` to [partial spec — file too large to fully analyze (2 parts)] save run metadata to disk.

**Request:**

```json
{
  "rootPath": "string",
  "metadata": "object"
}
```

#### Scenario: SaveRunMetadata
- **GIVEN** Root path and metadata
- **WHEN** The function is called
- **THEN** Saves the metadata to disk

### Requirement: FunctionUI

The API SHALL support `Function displayBanner(projectName: string, projectType: string, rootPath: string)` to [partial spec — file too large to fully analyze (2 parts)] display the pipeline banner with project information.

**Request:**

```json
{
  "projectName": "string",
  "projectType": "string",
  "rootPath": "string"
}
```

#### Scenario: DisplayBanner
- **GIVEN** Valid project name, type, and root path
- **WHEN** Function is called
- **THEN** Displays the banner with project information

### Requirement: FunctionUI

The API SHALL support `Function displayCompletionBanner()` to [partial spec — file too large to fully analyze (2 parts)] display the completion banner with next steps.

#### Scenario: DisplayCompletionBanner
- **GIVEN** None
- **WHEN** Function is called
- **THEN** Displays the completion banner with next steps

### Requirement: FunctionUI

The API SHALL support `Function displayBanner(projectName, projectType, rootPath)` to [partial spec — file too large to fully analyze (2 parts)] display the pipeline banner.

**Request:**

```json
{
  "projectName": "string",
  "projectType": "string",
  "rootPath": "string"
}
```

#### Scenario: DisplayBanner
- **GIVEN** Project name, project type, and root path
- **WHEN** The function is called
- **THEN** Displays the banner in the console

### Requirement: CommandSpecificationGenerationPipeline

The API SHALL support `Command spec-gen run` to [partial spec — file too large to fully analyze (2 parts)] run the full spec-gen pipeline (init → analyze → generate)

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "model": "string",
  "dryRun": "boolean",
  "yes": "boolean",
  "maxFiles": "number",
  "adr": "boolean"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "metadata": {
    "version": "string",
    "timestamp": "string",
    "duration": "number",
    "steps": {
      "init": {
        "status": "string",
        "reason": "string"
      },
      "analyze": {
        "status": "string",
        "reason": "string",
        "filesAnalyzed": "number"
      },
      "generate": {
        "status": "string",
        "specsGenerated": "number"
      }
    },
    "result": "string",
    "error": "string"
  }
}
```

#### Scenario: FullPipelineExecution
- **GIVEN** A project directory with source code
- **WHEN** The command 'spec-gen run' is executed
- **THEN** The pipeline completes successfully with generated specifications
- **AND** Initialization step creates configuration if needed
- **AND** Analysis step processes source files and builds dependency graph
- **AND** Generation step produces OpenSpec specifications

#### Scenario: DryRun
- **GIVEN** A project directory with source code
- **WHEN** The command 'spec-gen run --dry-run' is executed
- **THEN** The pipeline shows what would be done without making changes
- **AND** No files are modified
- **AND** Estimated cost is displayed
- **AND** Detailed preview of generation steps is shown

#### Scenario: ForceReinitialization
- **GIVEN** An existing spec-gen configuration
- **WHEN** The command 'spec-gen run --force' is executed
- **THEN** The initialization step recreates the configuration
- **AND** Existing configuration is overwritten
- **AND** New configuration is created with default values
- **AND** OpenSpec directory is recreated if needed

### Requirement: CommandCommand

The API SHALL support `Command view` to configure and execute the view command for the application.

#### Scenario: ExecuteViewCommand
- **GIVEN** The view command is invoked from the CLI
- **WHEN** The view command is executed
- **THEN** The server is configured and the view command is processed

### Requirement: CommandConfiguration

The API SHALL support `Command init` to initialize spec-gen in the current project by detecting project type, checking for existing configurations, creating .spec-gen configuration, updating .gitignore, and preparing the project for analysis.

**Request:**

```json
{
  "options": {
    "force": "boolean",
    "openspecPath": "string"
  }
}
```

**Response:**

```json
{
  "config": {
    "projectType": "string",
    "openspecPath": "string",
    "createdAt": "string",
    "lastRun": "string"
  },
  "messages": "string[]"
}
```

#### Scenario: InitializeWithDefaults
- **GIVEN** A valid project directory with a supported manifest file
- **WHEN** The 'init' command is executed without options
- **THEN** A new .spec-gen/config.json file is created with default settings and the openspec/ directory
structure is set up
- **AND** The project type is detected and logged
- **AND** The .gitignore file is updated to exclude .spec-gen/
- **AND** A success message is displayed with next steps

#### Scenario: OverwriteExistingConfiguration
- **GIVEN** An existing .spec-gen/config.json file
- **WHEN** The 'init' command is executed with the --force option
- **THEN** The existing configuration is overwritten with new settings
- **AND** The project type is re-detected and logged
- **AND** The openspec/ directory structure is preserved if it exists
- **AND** A success message is displayed with the updated configuration details

#### Scenario: CustomOutputPath
- **GIVEN** A valid project directory
- **WHEN** The 'init' command is executed with the --openspec-path option set to a custom path
- **THEN** The openspec/ directory is created at the specified custom path
- **AND** The configuration file is updated to reflect the custom path
- **AND** The .gitignore file is updated to exclude .spec-gen/
- **AND** A success message is displayed with the custom path details

### Requirement: FunctionConfiguration

The API SHALL support `Function specGenInit` to initialize spec-gen in a project directory.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "openspecPath": "string",
    "force": "boolean",
    "onProgress": "function"
  }
}
```

**Response:**

```json
{
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string",
  "created": "boolean"
}
```

#### Scenario: InitializeSpecGen
- **GIVEN** A project directory with no existing spec-gen configuration
- **WHEN** specGenInit is called with default options
- **THEN** Returns an object with configPath, openspecPath, projectType, and created set to true
- **AND** Creates .spec-gen/config.json
- **AND** Creates the openspec/ directory structure
- **AND** Updates .gitignore

#### Scenario: SkipInitialization
- **GIVEN** A project directory with an existing spec-gen configuration
- **WHEN** specGenInit is called with default options
- **THEN** Returns an object with configPath, openspecPath, projectType, and created set to false
- **AND** Does not create .spec-gen/config.json
- **AND** Does not create the openspec/ directory structure
- **AND** Does not update .gitignore

#### Scenario: ForceInitialization
- **GIVEN** A project directory with an existing spec-gen configuration
- **WHEN** specGenInit is called with force option set to true
- **THEN** Returns an object with configPath, openspecPath, projectType, and created set to true
- **AND** Overwrites .spec-gen/config.json
- **AND** Creates the openspec/ directory structure if it doesn't exist
- **AND** Updates .gitignore if necessary

### Requirement: CLICommandConfiguration

The API SHALL support `CLI Command spec-gen init` to initialize spec-gen in the current project by detecting project type, creating configuration, and preparing for analysis.

**Request:**

```json
{
  "options": {
    "force": "boolean",
    "openspec-path": "string"
  }
}
```

**Response:**

```json
{
  "status": "string",
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string"
}
```

#### Scenario: InitializeWithDefaults
- **GIVEN** Current directory is a valid project root
- **WHEN** spec-gen init is executed
- **THEN** Creates .spec-gen/config.json and openspec/ directory structure
- **AND** Detects project type
- **AND** Updates .gitignore if needed
- **AND** Outputs initialization summary

#### Scenario: OverwriteExistingConfig
- **GIVEN** Existing .spec-gen/config.json exists
- **WHEN** spec-gen init --force is executed
- **THEN** Overwrites existing configuration
- **AND** Preserves existing OpenSpec setup if present
- **AND** Updates .gitignore if needed

#### Scenario: CustomOutputPath
- **GIVEN** Current directory is a valid project root
- **WHEN** spec-gen init --openspec-path ./docs/specs is executed
- **THEN** Creates configuration with custom OpenSpec output path
- **AND** Creates directory structure at specified path
- **AND** Updates configuration accordingly

### Requirement: FunctionConfiguration

The API SHALL support `Function checkConfig` to [partial spec — file too large to fully analyze (2 parts)] check if the spec-gen configuration file exists and is valid.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: ConfigCheck
- **GIVEN** The spec-gen configuration file exists and is valid
- **WHEN** checkConfig is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating the configuration file exists and the project type

#### Scenario: ConfigCheck
- **GIVEN** The spec-gen configuration file exists but is invalid
- **WHEN** checkConfig is called with the rootPath
- **THEN** Returns status 'fail' with a message indicating the configuration file could not be parsed and a fix
message

#### Scenario: ConfigCheck
- **GIVEN** The spec-gen configuration file does not exist
- **WHEN** checkConfig is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating the configuration file not found and a fix message

### Requirement: CLICommandSpecificationVerification

The API SHALL support `CLI Command verify` to [partial spec — file too large to fully analyze (2 parts)] verify the accuracy of generated specifications against the actual codebase.

**Request:**

```json
{
  "files": "string[]",
  "domains": "string[]",
  "json": "boolean"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "summary": "string"
}
```

#### Scenario: VerifySpecifications
- **GIVEN** A codebase with generated specifications
- **WHEN** The verify command is executed
- **THEN** A verification report is generated and displayed
- **AND** The report includes overall confidence score
- **AND** The report includes domain-specific accuracy breakdowns
- **AND** The report includes identified gaps and suggested improvements

### Requirement: CLICommandSpecificationDrift

The API SHALL support `CLI Command spec-gen drift` to [partial spec — file too large to fully analyze (2 parts)] detects and reports specification drift between code and specifications.

**Request:**

```json
{
  "verbose": "boolean",
  "failOn": "string",
  "json": "boolean",
  "installHook": "boolean",
  "uninstallHook": "boolean"
}
```

**Response:**

```json
{
  "issues": "DriftIssue[]",
  "summary": {
    "gaps": "number",
    "stale": "number",
    "uncovered": "number",
    "orphanedSpecs": "number",
    "adrGaps": "number",
    "adrOrphaned": "number"
  }
}
```

#### Scenario: DetectDrift
- **GIVEN** A project with code and specifications
- **WHEN** The 'spec-gen drift' command is executed
- **THEN** A list of drift issues is displayed
- **AND** The summary of drift issues is shown
- **AND** The command exits with a non-zero status if issues are found

#### Scenario: InstallPreCommitHook
- **GIVEN** A git repository
- **WHEN** The 'spec-gen drift --install-hook' command is executed
- **THEN** A pre-commit hook is installed
- **AND** The hook checks for specification drift before each commit

#### Scenario: UninstallPreCommitHook
- **GIVEN** An installed pre-commit hook
- **WHEN** The 'spec-gen drift --uninstall-hook' command is executed
- **THEN** The pre-commit hook is removed

### Requirement: FunctionOpenSpec

The API SHALL support `Function specGenGenerate` to [partial spec — file too large to fully analyze (2 parts)] generates openspec specification files from analysis results using llm.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "analysisPath": "string",
    "onProgress": "function",
    "provider": "string",
    "model": "string",
    "openaiCompatBaseUrl": "string",
    "apiBase": "string",
    "sslVerify": "boolean",
    "dryRun": "boolean",
    "adr": "boolean",
    "adrOnly": "boolean",
    "mapping": "boolean",
    "domains": "string[]",
    "writeMode": "string"
  }
}
```

**Response:**

```json
{
  "report": {
    "timestamp": "string",
    "openspecVersion": "string",
    "specGenVersion": "string",
    "filesWritten": "string[]",
    "filesSkipped": "string[]",
    "filesBackedUp": "string[]",
    "filesMerged": "string[]",
    "configUpdated": "boolean",
    "validationErrors": "string[]",
    "warnings": "string[]",
    "nextSteps": "string[]"
  },
  "pipelineResult": "object",
  "duration": "number"
}
```

#### Scenario: GenerateOpenSpecFiles
- **GIVEN** Valid configuration and analysis data
- **WHEN** specGenGenerate is called
- **THEN** OpenSpec files are generated and written to the specified directory
- **AND** LLM logs are saved
- **AND** A report is returned with details of the operation

#### Scenario: DryRun
- **GIVEN** Valid configuration and analysis data with dryRun option set to true
- **WHEN** specGenGenerate is called
- **THEN** No files are written, but a report is returned with next steps
- **AND** No LLM logs are saved

#### Scenario: GenerateADRsOnly
- **GIVEN** Valid configuration and analysis data with adrOnly option set to true
- **WHEN** specGenGenerate is called
- **THEN** Only ADR files are generated and written to the specified directory
- **AND** LLM logs are saved
- **AND** A report is returned with details of the operation

### Requirement: FunctionOpenSpec

The API SHALL support `Function checkOpenSpecDir` to [partial spec — file too large to fully analyze (2 parts)] check if the openspec directory exists.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: OpenSpecDirectoryCheck
- **GIVEN** The OpenSpec directory exists
- **WHEN** checkOpenSpecDir is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating the OpenSpec directory exists

#### Scenario: OpenSpecDirectoryCheck
- **GIVEN** The OpenSpec directory does not exist
- **WHEN** checkOpenSpecDir is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating the OpenSpec directory not found and a fix message

### Requirement: FunctionUserInteraction

The API SHALL support `Function promptConfirmation(message, autoYes)` to [partial spec — file too large to fully analyze (2 parts)] prompt user for confirmation. uses @inquirer/prompts in tty, auto-yes otherwise.

**Request:**

```json
{
  "message": "string",
  "autoYes": "boolean"
}
```

**Response:**

```json
{
  "confirmation": "boolean"
}
```

#### Scenario: PromptUserForConfirmation
- **GIVEN** Message and autoYes flag
- **WHEN** Function is called with message and autoYes
- **THEN** Returns user confirmation or auto-yes response

### Requirement: GETAnalysisArtifact

The API SHALL support `GET /api/analysis/artifacts` to [partial spec — file too large to fully analyze (2 parts)] load cached analysis artifacts from disk.

**Response:**

```json
{
  "repoStructure": "object",
  "summaryMarkdown": "string",
  "dependencyDiagram": "string",
  "llmContext": "object"
}
```

#### Scenario: LoadCachedArtifacts
- **GIVEN** Analysis path and repository structure
- **WHEN** GET /api/analysis/artifacts is called
- **THEN** 200 OK with cached artifacts in response body

### Requirement: POSTSpecGenPipeline

The API SHALL support `POST /api/spec-gen/run` to [partial spec — file too large to fully analyze (2 parts)] run the full spec-gen pipeline including initialization, analysis, and generation of specifications.

**Request:**

```json
{
  "rootPath": "string",
  "force": "boolean",
  "reanalyze": "boolean",
  "maxFiles": "number",
  "adr": "boolean",
  "dryRun": "boolean",
  "provider": "string",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string"
}
```

**Response:**

```json
{
  "init": {
    "configPath": "string",
    "openspecPath": "string",
    "projectType": "string",
    "created": "boolean"
  },
  "analysis": {
    "repoMap": "object",
    "depGraph": "object",
    "artifacts": "object",
    "duration": "number"
  },
  "generation": {
    "report": "object",
    "pipelineResult": "object",
    "duration": "number"
  },
  "duration": "number"
}
```

#### Scenario: FullPipelineRun
- **GIVEN** A project directory with valid configuration
- **WHEN** POST /api/spec-gen/run is called with valid parameters
- **THEN** 200 OK with the full pipeline result including initialization, analysis, and generation steps

#### Scenario: DryRun
- **GIVEN** A project directory with valid configuration
- **WHEN** POST /api/spec-gen/run is called with dryRun=true
- **THEN** 200 OK with a report of what would be generated without actually writing files

#### Scenario: MissingLLMAPIKey
- **GIVEN** A project directory without any LLM API keys set
- **WHEN** POST /api/spec-gen/run is called
- **THEN** 400 Bad Request with an error message about missing API keys

### Requirement: FunctionErrorMessage

The API SHALL support `function sanitizeErrorMessage` to [partial spec — file too large to fully analyze (2 parts)] strip internal filesystem paths and api keys from error messages before sending to clients.

#### Scenario: SanitizeErrorMessage
- **GIVEN** An error message containing sensitive information
- **WHEN** The sanitizeErrorMessage function is called
- **THEN** The error message is sanitized and returned without sensitive information

### Requirement: FunctionPath

The API SHALL support `function safePath` to [partial spec — file too large to fully analyze (2 parts)] ensure a resolved path stays within the project root. returns null if invalid.

#### Scenario: ResolveSafePath
- **GIVEN** A root path and a user-provided path
- **WHEN** The safePath function is called
- **THEN** The resolved path is checked to ensure it stays within the project root, and the function returns
the resolved path if valid or null if invalid

### Requirement: FunctionURL

The API SHALL support `function openBrowser` to [partial spec — file too large to fully analyze (2 parts)] open a url in the default browser.

#### Scenario: OpenURLInBrowser
- **GIVEN** A URL to open
- **WHEN** The openBrowser function is called
- **THEN** The URL is opened in the default browser

### Requirement: GETDependencyGraph

The API SHALL support `GET /api/dependency-graph` to [partial spec — file too large to fully analyze (2 parts)] retrieve the dependency graph data.

**Response:**

```json
{
  "graph": "object"
}
```

#### Scenario: GetDependencyGraph
- **GIVEN** Analysis directory exists
- **WHEN** GET /api/dependency-graph is called
- **THEN** 200 OK with graph data in response body

### Requirement: GETLLMContext

The API SHALL support `GET /api/llm-context` to [partial spec — file too large to fully analyze (2 parts)] retrieve the llm context data.

**Response:**

```json
{
  "context": "object"
}
```

#### Scenario: GetLLMContext
- **GIVEN** LLM context file exists
- **WHEN** GET /api/llm-context is called
- **THEN** 200 OK with context data in response body

### Requirement: GETClassGraph

The API SHALL support `GET /api/class-graph` to [partial spec — file too large to fully analyze (2 parts)] retrieve the class graph data.

**Response:**

```json
{
  "classes": "array",
  "inheritanceEdges": "array",
  "edges": "array",
  "nodes": "array"
}
```

#### Scenario: GetClassGraph
- **GIVEN** LLM context file exists
- **WHEN** GET /api/class-graph is called
- **THEN** 200 OK with class graph data in response body

### Requirement: GETRefactorPriorities

The API SHALL support `GET /api/refactor-priorities` to [partial spec — file too large to fully analyze (2 parts)] retrieve the refactor priorities data.

**Response:**

```json
{
  "priorities": "object"
}
```

#### Scenario: GetRefactorPriorities
- **GIVEN** Refactor priorities file exists
- **WHEN** GET /api/refactor-priorities is called
- **THEN** 200 OK with priorities data in response body

### Requirement: GETMapping

The API SHALL support `GET /api/mapping` to [partial spec — file too large to fully analyze (2 parts)] retrieve the mapping data.

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetMapping
- **GIVEN** Mapping file exists
- **WHEN** GET /api/mapping is called
- **THEN** 200 OK with mapping data in response body

### Requirement: POSTMapping

The API SHALL support `POST /api/mcp/mapping` to [partial spec — file too large to fully analyze (4 parts)] generate a mapping for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "mapping": "Mapping"
}
```

#### Scenario: GenerateMapping
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/mapping is called
- **THEN** 200 OK with mapping data in response body

### Requirement: MCPMapping

The API SHALL support `MCP getMapping` to [partial spec — file too large to fully analyze (4 parts)] get the mapping between code and specifications.

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetMapping
- **GIVEN** MCP server is running
- **WHEN** getMapping is called
- **THEN** Returns the mapping between code and specifications

### Requirement: GETSpecificationRequirement

The API SHALL support `GET /api/spec-requirements` to [partial spec — file too large to fully analyze (2 parts)] retrieve the specification requirements.

**Response:**

```json
{
  "requirements": "object"
}
```

#### Scenario: GetSpecRequirements
- **GIVEN** Mapping file exists
- **WHEN** GET /api/spec-requirements is called
- **THEN** 200 OK with requirements data in response body

### Requirement: GETFileSkeleton

The API SHALL support `GET /api/skeleton` to [partial spec — file too large to fully analyze (2 parts)] retrieve the skeleton content of a file.

**Request:**

```json
{
  "file": "string"
}
```

**Response:**

```json
{
  "filePath": "string",
  "language": "string",
  "originalLines": "number",
  "skeletonLines": "number",
  "reductionPct": "number",
  "skeleton": "string"
}
```

#### Scenario: GetSkeletonContent
- **GIVEN** File exists and is accessible
- **WHEN** GET /api/skeleton?file=<path> is called
- **THEN** 200 OK with skeleton content in response body

### Requirement: POSTChat

The API SHALL support `POST /api/chat` to [partial spec — file too large to fully analyze (2 parts)] send a chat message and receive a response.

**Request:**

```json
{
  "message": "string",
  "history": "array",
  "model": "string"
}
```

**Response:**

```json
{
  "type": "string",
  "reply": "string",
  "highlightIds": "array",
  "filePaths": "array"
}
```

#### Scenario: SendChatMessage
- **GIVEN** Valid message and history
- **WHEN** POST /api/chat is called with message and history
- **THEN** 200 OK with reply, highlightIds, and filePaths in response body

### Requirement: GETChatModel

The API SHALL support `GET /api/chat/models` to [partial spec — file too large to fully analyze (2 parts)] retrieve the available chat models.

**Response:**

```json
{
  "provider": "string",
  "currentModel": "string",
  "models": "array"
}
```

#### Scenario: GetChatModels
- **GIVEN** Valid provider configuration
- **WHEN** GET /api/chat/models is called
- **THEN** 200 OK with provider, currentModel, and models in response body

### Requirement: GETSearch

The API SHALL support `GET /api/search` to [partial spec — file too large to fully analyze (2 parts)] search for code or documentation.

**Request:**

```json
{
  "q": "string"
}
```

**Response:**

```json
{
  "results": "array"
}
```

#### Scenario: SearchForCodeOrDocumentation
- **GIVEN** Valid query and vector index exists
- **WHEN** GET /api/search?q=<query> is called
- **THEN** 200 OK with search results in response body

### Requirement: FunctionNode.js

The API SHALL support `Function checkNodeVersion` to [partial spec — file too large to fully analyze (2 parts)] check the node.js version against the minimum required version.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: Node.jsVersionCheck
- **GIVEN** Node.js version is at least the minimum required version
- **WHEN** checkNodeVersion is called
- **THEN** Returns status 'ok' with the current Node.js version

#### Scenario: Node.jsVersionCheck
- **GIVEN** Node.js version is below the minimum required version
- **WHEN** checkNodeVersion is called
- **THEN** Returns status 'fail' with the current Node.js version and a fix message

### Requirement: FunctionGit

The API SHALL support `Function checkGit` to [partial spec — file too large to fully analyze (2 parts)] check if the project is a git repository and if git is available.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: GitRepositoryCheck
- **GIVEN** The project is a Git repository and Git is available
- **WHEN** checkGit is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating Git repository detected

#### Scenario: GitRepositoryCheck
- **GIVEN** The project is not a Git repository
- **WHEN** checkGit is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating no .git directory found and a fix message

#### Scenario: GitRepositoryCheck
- **GIVEN** The project is a Git repository but Git is not available
- **WHEN** checkGit is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating .git found but git binary not on PATH and a fix
message

### Requirement: FunctionLLMProvider

The API SHALL support `Function checkLLMProvider` to [partial spec — file too large to fully analyze (2 parts)] check if an llm provider is configured.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: LLMProviderCheck
- **GIVEN** Anthropic API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating ANTHROPIC_API_KEY is set

#### Scenario: LLMProviderCheck
- **GIVEN** OpenAI API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating OPENAI_API_KEY is set

#### Scenario: LLMProviderCheck
- **GIVEN** Gemini API key is set
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating GEMINI_API_KEY is set

#### Scenario: LLMProviderCheck
- **GIVEN** Claude CLI is available
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'ok' with a message indicating claude CLI detected

#### Scenario: LLMProviderCheck
- **GIVEN** SPEC_GEN_API_BASE is set but no API key
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'warn' with a message indicating SPEC_GEN_API_BASE is set but no API key

#### Scenario: LLMProviderCheck
- **GIVEN** No LLM provider is configured
- **WHEN** checkLLMProvider is called
- **THEN** Returns status 'fail' with a message indicating no LLM provider configured and a fix message

### Requirement: FunctionDiskSpace

The API SHALL support `Function checkDiskSpace` to [partial spec — file too large to fully analyze (2 parts)] check the available disk space.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is sufficient
- **WHEN** checkDiskSpace is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating the available disk space

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is low
- **WHEN** checkDiskSpace is called with the rootPath
- **THEN** Returns status 'warn' with a message indicating the available disk space is low and a fix message

#### Scenario: DiskSpaceCheck
- **GIVEN** Available disk space is very low
- **WHEN** checkDiskSpace is called with the rootPath
- **THEN** Returns status 'fail' with a message indicating the available disk space is very low and a fix
message

#### Scenario: DiskSpaceCheck
- **GIVEN** df command is not available
- **WHEN** checkDiskSpace is called with the rootPath
- **THEN** Returns status 'ok' with a message indicating the check was skipped

### Requirement: GETEnvironment

The API SHALL support `GET /doctor` to [partial spec — file too large to fully analyze (2 parts)] check your environment and configuration for common issues

**Response:**

```json
{
  "checks": [
    {
      "name": "string",
      "status": "string",
      "detail": "string",
      "fix": "string"
    }
  ]
}
```

#### Scenario: RunAllChecks
- **GIVEN** User runs the doctor command
- **WHEN** The command is executed
- **THEN** All checks are performed and results are displayed
- **AND** If there are failures, an error message is displayed
- **AND** If there are warnings, a warning message is displayed
- **AND** If all checks pass, a success message is displayed

#### Scenario: OutputResultsAsJSON
- **GIVEN** User runs the doctor command with the --json option
- **WHEN** The command is executed
- **THEN** All checks are performed and results are output as JSON
- **AND** The JSON output includes the status, detail, and fix for each check

### Requirement: POSTCallGraph

The API SHALL support `POST /api/mcp/call-graph` to [partial spec — file too large to fully analyze (4 parts)] generate a call graph for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "nodes": "Node[]",
  "edges": "Edge[]"
}
```

#### Scenario: GenerateCallGraph
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/call-graph is called
- **THEN** 200 OK with call graph data in response body

### Requirement: MCPCallGraph

The API SHALL support `MCP getCallGraph` to [partial spec — file too large to fully analyze (4 parts)] get the call graph for a given function or file.

**Request:**

```json
{
  "functionName": "string",
  "filePath": "string"
}
```

**Response:**

```json
{
  "callGraph": "object"
}
```

#### Scenario: GetCallGraph
- **GIVEN** MCP server is running and the function or file exists
- **WHEN** getCallGraph is called with the function name or file path
- **THEN** Returns the call graph for the specified function or file

### Requirement: MCPCallGraph

The API SHALL support `MCP getSubgraph` to [partial spec — file too large to fully analyze (4 parts)] get a subgraph of the call graph for a given function or file.

**Request:**

```json
{
  "functionName": "string",
  "filePath": "string",
  "depth": "number"
}
```

**Response:**

```json
{
  "subgraph": "object"
}
```

#### Scenario: GetSubgraph
- **GIVEN** MCP server is running and the function or file exists
- **WHEN** getSubgraph is called with the function name or file path and depth
- **THEN** Returns the subgraph of the call graph for the specified function or file

### Requirement: POSTSubgraph

The API SHALL support `POST /api/mcp/subgraph` to [partial spec — file too large to fully analyze (4 parts)] generate a subgraph for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]",
  "depth": "number"
}
```

**Response:**

```json
{
  "nodes": "Node[]",
  "edges": "Edge[]"
}
```

#### Scenario: GenerateSubgraph
- **GIVEN** Valid directory, entry points, and depth
- **WHEN** POST /api/mcp/subgraph is called
- **THEN** 200 OK with subgraph data in response body

### Requirement: POSTImpactAnalysis

The API SHALL support `POST /api/mcp/analyze-impact` to [partial spec — file too large to fully analyze (4 parts)] analyze the impact of changes in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]",
  "changes": "Change[]"
}
```

**Response:**

```json
{
  "impactedFunctions": "Function[]",
  "impactScore": "number"
}
```

#### Scenario: AnalyzeImpact
- **GIVEN** Valid directory, entry points, and changes
- **WHEN** POST /api/mcp/analyze-impact is called
- **THEN** 200 OK with impact analysis data in response body

### Requirement: MCPImpactAnalysis

The API SHALL support `MCP analyzeImpact` to [partial spec — file too large to fully analyze (4 parts)] analyze the impact of a change in the codebase.

**Request:**

```json
{
  "change": "object"
}
```

**Response:**

```json
{
  "impactAnalysis": "object"
}
```

#### Scenario: AnalyzeImpact
- **GIVEN** MCP server is running
- **WHEN** analyzeImpact is called with the change details
- **THEN** Returns the impact analysis of the change

### Requirement: POSTRefactorCandidate

The API SHALL support `POST /api/mcp/low-risk-refactor-candidates` to [partial spec — file too large to fully analyze (4 parts)] identify low-risk refactor candidates in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "candidates": "Function[]"
}
```

#### Scenario: IdentifyLowRiskRefactorCandidates
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/low-risk-refactor-candidates is called
- **THEN** 200 OK with refactor candidates data in response body

### Requirement: MCPRefactorCandidate

The API SHALL support `MCP getLowRiskRefactorCandidates` to [partial spec — file too large to fully analyze (4 parts)] get low-risk refactor candidates in the codebase.

**Response:**

```json
{
  "refactorCandidates": "array"
}
```

#### Scenario: GetLowRiskRefactorCandidates
- **GIVEN** MCP server is running
- **WHEN** getLowRiskRefactorCandidates is called
- **THEN** Returns a list of low-risk refactor candidates

### Requirement: POSTFunction

The API SHALL support `POST /api/mcp/leaf-functions` to [partial spec — file too large to fully analyze (4 parts)] identify leaf functions in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "functions": "Function[]"
}
```

#### Scenario: IdentifyLeafFunctions
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/leaf-functions is called
- **THEN** 200 OK with leaf functions data in response body

### Requirement: POSTFunction

The API SHALL support `POST /api/mcp/critical-hubs` to [partial spec — file too large to fully analyze (4 parts)] identify critical hubs in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "hubs": "Function[]"
}
```

#### Scenario: IdentifyCriticalHubs
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/critical-hubs is called
- **THEN** 200 OK with critical hubs data in response body

### Requirement: POSTFunction

The API SHALL support `POST /api/mcp/god-functions` to [partial spec — file too large to fully analyze (4 parts)] identify god functions in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "functions": "Function[]"
}
```

#### Scenario: IdentifyGodFunctions
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/god-functions is called
- **THEN** 200 OK with god functions data in response body

### Requirement: MCPFunction

The API SHALL support `MCP getLeafFunctions` to [partial spec — file too large to fully analyze (4 parts)] get leaf functions in the codebase.

**Response:**

```json
{
  "leafFunctions": "array"
}
```

#### Scenario: GetLeafFunctions
- **GIVEN** MCP server is running
- **WHEN** getLeafFunctions is called
- **THEN** Returns a list of leaf functions

### Requirement: MCPFunction

The API SHALL support `MCP getGodFunctions` to [partial spec — file too large to fully analyze (4 parts)] get god functions in the codebase.

**Response:**

```json
{
  "godFunctions": "array"
}
```

#### Scenario: GetGodFunctions
- **GIVEN** MCP server is running
- **WHEN** getGodFunctions is called
- **THEN** Returns a list of god functions

### Requirement: MCPFunction

The API SHALL support `MCP getFunctionSkeleton` to [partial spec — file too large to fully analyze (4 parts)] get the skeleton of a function.

**Request:**

```json
{
  "functionName": "string"
}
```

**Response:**

```json
{
  "skeleton": "object"
}
```

#### Scenario: GetFunctionSkeleton
- **GIVEN** MCP server is running and the function exists
- **WHEN** getFunctionSkeleton is called with the function name
- **THEN** Returns the function skeleton

### Requirement: MCPFunction

The API SHALL support `MCP getFunctionBody` to [partial spec — file too large to fully analyze (4 parts)] get the body of a function.

**Request:**

```json
{
  "functionName": "string"
}
```

**Response:**

```json
{
  "body": "object"
}
```

#### Scenario: GetFunctionBody
- **GIVEN** MCP server is running and the function exists
- **WHEN** getFunctionBody is called with the function name
- **THEN** Returns the function body

### Requirement: POSTDependency

The API SHALL support `POST /api/mcp/file-dependencies` to [partial spec — file too large to fully analyze (4 parts)] identify file dependencies in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "dependencies": "Dependency[]"
}
```

#### Scenario: IdentifyFileDependencies
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/file-dependencies is called
- **THEN** 200 OK with file dependencies data in response body

### Requirement: POSTExecutionPath

The API SHALL support `POST /api/mcp/trace-execution-path` to [partial spec — file too large to fully analyze (4 parts)] trace the execution path of a function in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "functionName": "string"
}
```

**Response:**

```json
{
  "path": "Path[]"
}
```

#### Scenario: TraceExecutionPath
- **GIVEN** Valid directory and function name
- **WHEN** POST /api/mcp/trace-execution-path is called
- **THEN** 200 OK with execution path data in response body

### Requirement: MCPExecutionPath

The API SHALL support `MCP traceExecutionPath` to [partial spec — file too large to fully analyze (4 parts)] trace the execution path of a given function or file.

**Request:**

```json
{
  "functionName": "string",
  "filePath": "string"
}
```

**Response:**

```json
{
  "executionPath": "array"
}
```

#### Scenario: TraceExecutionPath
- **GIVEN** MCP server is running and the function or file exists
- **WHEN** traceExecutionPath is called with the function name or file path
- **THEN** Returns the execution path of the specified function or file

### Requirement: POSTCodebaseAnalysis

The API SHALL support `POST /api/mcp/analyze-codebase` to [partial spec — file too large to fully analyze (4 parts)] analyze the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "analysis": "Analysis"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/analyze-codebase is called
- **THEN** 200 OK with analysis data in response body

### Requirement: MCPCodebaseAnalysis

The API SHALL support `MCP analyzeCodebase` to [partial spec — file too large to fully analyze (4 parts)] analyze the codebase for architectural issues.

**Response:**

```json
{
  "analysis": "object"
}
```

#### Scenario: AnalyzeCodebase
- **GIVEN** MCP server is running
- **WHEN** analyzeCodebase is called
- **THEN** Returns the analysis results

### Requirement: POSTArchitectureOverview

The API SHALL support `POST /api/mcp/architecture-overview` to [partial spec — file too large to fully analyze (4 parts)] generate an architecture overview for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "overview": "Overview"
}
```

#### Scenario: GenerateArchitectureOverview
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/architecture-overview is called
- **THEN** 200 OK with architecture overview data in response body

### Requirement: POSTRefactorReport

The API SHALL support `POST /api/mcp/refactor-report` to [partial spec — file too large to fully analyze (4 parts)] generate a refactor report for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "report": "Report"
}
```

#### Scenario: GenerateRefactorReport
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/refactor-report is called
- **THEN** 200 OK with refactor report data in response body

### Requirement: MCPRefactorReport

The API SHALL support `MCP getRefactorReport` to [partial spec — file too large to fully analyze (4 parts)] get a refactor report for the codebase.

**Response:**

```json
{
  "report": "object"
}
```

#### Scenario: GetRefactorReport
- **GIVEN** MCP server is running
- **WHEN** getRefactorReport is called
- **THEN** Returns the refactor report

### Requirement: POSTDuplicateReport

The API SHALL support `POST /api/mcp/duplicate-report` to [partial spec — file too large to fully analyze (4 parts)] generate a duplicate report for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "report": "Report"
}
```

#### Scenario: GenerateDuplicateReport
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/duplicate-report is called
- **THEN** 200 OK with duplicate report data in response body

### Requirement: MCPDuplicateReport

The API SHALL support `MCP getDuplicateReport` to [partial spec — file too large to fully analyze (4 parts)] get a duplicate code report for the codebase.

**Response:**

```json
{
  "report": "object"
}
```

#### Scenario: GetDuplicateReport
- **GIVEN** MCP server is running
- **WHEN** getDuplicateReport is called
- **THEN** Returns the duplicate code report

### Requirement: POSTSignature

The API SHALL support `POST /api/mcp/signatures` to [partial spec — file too large to fully analyze (4 parts)] generate signatures for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]"
}
```

**Response:**

```json
{
  "signatures": "Signature[]"
}
```

#### Scenario: GenerateSignatures
- **GIVEN** Valid directory and entry points
- **WHEN** POST /api/mcp/signatures is called
- **THEN** 200 OK with signatures data in response body

### Requirement: POSTSpecDrift

The API SHALL support `POST /api/mcp/check-spec-drift` to [partial spec — file too large to fully analyze (4 parts)] check for specification drift in the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "entryPoints": "string[]",
  "maxFiles": "number"
}
```

**Response:**

```json
{
  "drift": "Drift[]"
}
```

#### Scenario: CheckSpecificationDrift
- **GIVEN** Valid directory, entry points, and max files
- **WHEN** POST /api/mcp/check-spec-drift is called
- **THEN** 200 OK with specification drift data in response body

### Requirement: POSTFunctionSkeleton

The API SHALL support `POST /api/mcp/function-skeleton` to [partial spec — file too large to fully analyze (4 parts)] generate a function skeleton for the specified codebase.

**Request:**

```json
{
  "directory": "string",
  "functionName": "string"
}
```

**Response:**

```json
{
  "skeleton": "Skeleton"
}
```

#### Scenario: GenerateFunctionSkeleton
- **GIVEN** Valid directory and function name
- **WHEN** POST /api/mcp/function-skeleton is called
- **THEN** 200 OK with function skeleton data in response body

### Requirement: POSTTool

The API SHALL support `POST /api/mcp/tools` to [partial spec — file too large to fully analyze (4 parts)] list available tools and their definitions.

**Response:**

```json
{
  "tools": "array"
}
```

#### Scenario: ListTools
- **GIVEN** MCP server is running
- **WHEN** POST /api/mcp/tools is called
- **THEN** 200 OK with list of tools in response body

### Requirement: POSTTool

The API SHALL support `POST /api/mcp/call` to [partial spec — file too large to fully analyze (4 parts)] call a specific tool with provided arguments.

**Request:**

```json
{
  "name": "string",
  "arguments": "object"
}
```

**Response:**

```json
{
  "content": "array",
  "isError": "boolean"
}
```

#### Scenario: CallOrientTool
- **GIVEN** Valid directory and task
- **WHEN** POST /api/mcp/call with name='orient' and arguments
- **THEN** 200 OK with tool result in response body

#### Scenario: CallAnalyzeCodebaseTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='analyze_codebase' and arguments
- **THEN** 200 OK with analysis result in response body

#### Scenario: CallGetArchitectureOverviewTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_architecture_overview' and arguments
- **THEN** 200 OK with architecture overview in response body

#### Scenario: CallGetRefactorReportTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_refactor_report' and arguments
- **THEN** 200 OK with refactor report in response body

#### Scenario: CallGetCallGraphTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_call_graph' and arguments
- **THEN** 200 OK with call graph in response body

#### Scenario: CallGetSignaturesTool
- **GIVEN** Valid directory and file pattern
- **WHEN** POST /api/mcp/call with name='get_signatures' and arguments
- **THEN** 200 OK with function signatures in response body

#### Scenario: CallGetSubgraphTool
- **GIVEN** Valid directory and function name
- **WHEN** POST /api/mcp/call with name='get_subgraph' and arguments
- **THEN** 200 OK with subgraph in response body

#### Scenario: CallTraceExecutionPathTool
- **GIVEN** Valid directory, entry function, and target function
- **WHEN** POST /api/mcp/call with name='trace_execution_path' and arguments
- **THEN** 200 OK with execution path in response body

#### Scenario: CallGetMappingTool
- **GIVEN** Valid directory and domain
- **WHEN** POST /api/mcp/call with name='get_mapping' and arguments
- **THEN** 200 OK with mapping in response body

#### Scenario: CallAnalyzeImpactTool
- **GIVEN** Valid directory and symbol
- **WHEN** POST /api/mcp/call with name='analyze_impact' and arguments
- **THEN** 200 OK with impact analysis in response body

#### Scenario: CallGetLowRiskRefactorCandidatesTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_low_risk_refactor_candidates' and arguments
- **THEN** 200 OK with low risk refactor candidates in response body

#### Scenario: CallGetLeafFunctionsTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_leaf_functions' and arguments
- **THEN** 200 OK with leaf functions in response body

#### Scenario: CallGetCriticalHubsTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_critical_hubs' and arguments
- **THEN** 200 OK with critical hubs in response body

#### Scenario: CallGetDuplicateReportTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='get_duplicate_report' and arguments
- **THEN** 200 OK with duplicate report in response body

#### Scenario: CallGetFunctionSkeletonTool
- **GIVEN** Valid directory and file path
- **WHEN** POST /api/mcp/call with name='get_function_skeleton' and arguments
- **THEN** 200 OK with function skeleton in response body

#### Scenario: CallGetGodFunctionsTool
- **GIVEN** Valid directory and file path
- **WHEN** POST /api/mcp/call with name='get_god_functions' and arguments
- **THEN** 200 OK with god functions in response body

#### Scenario: CallCheckSpecDriftTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='check_spec_drift' and arguments
- **THEN** 200 OK with spec drift report in response body

#### Scenario: CallSearchCodeTool
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/call with name='search_code' and arguments
- **THEN** 200 OK with search results in response body

#### Scenario: CallSuggestInsertionPointsTool
- **GIVEN** Valid directory and description
- **WHEN** POST /api/mcp/call with name='suggest_insertion_points' and arguments
- **THEN** 200 OK with insertion points in response body

#### Scenario: CallSearchSpecsTool
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/call with name='search_specs' and arguments
- **THEN** 200 OK with spec search results in response body

#### Scenario: CallListSpecDomainsTool
- **GIVEN** Valid directory
- **WHEN** POST /api/mcp/call with name='list_spec_domains' and arguments
- **THEN** 200 OK with list of spec domains in response body

#### Scenario: CallGetSpecTool
- **GIVEN** Valid directory and domain
- **WHEN** POST /api/mcp/call with name='get_spec' and arguments
- **THEN** 200 OK with spec in response body

#### Scenario: CallGetFunctionBodyTool
- **GIVEN** Valid directory, file path, and function name
- **WHEN** POST /api/mcp/call with name='get_function_body' and arguments
- **THEN** 200 OK with function body in response body

#### Scenario: CallGetFileDependenciesTool
- **GIVEN** Valid directory and file path
- **WHEN** POST /api/mcp/call with name='get_file_dependencies' and arguments
- **THEN** 200 OK with file dependencies in response body

#### Scenario: CallGetDecisionsTool
- **GIVEN** Valid directory and query
- **WHEN** POST /api/mcp/call with name='get_decisions' and arguments
- **THEN** 200 OK with decisions in response body

### Requirement: MCPTool

The API SHALL support `MCP listTools` to [partial spec — file too large to fully analyze (4 parts)] list available tools provided by the spec-gen mcp server.

**Response:**

```json
{
  "tools": [
    {
      "name": "string",
      "description": "string",
      "parameters": {
        "type": "object",
        "properties": {}
      }
    }
  ]
}
```

#### Scenario: ListTools
- **GIVEN** MCP server is running
- **WHEN** listTools is called
- **THEN** Returns a list of available tools with their descriptions and parameters

### Requirement: MCPTool

The API SHALL support `MCP callTool` to [partial spec — file too large to fully analyze (4 parts)] call a specific tool provided by the spec-gen mcp server.

**Request:**

```json
{
  "tool": "string",
  "parameters": "object"
}
```

**Response:**

```json
{
  "result": "object"
}
```

#### Scenario: CallTool
- **GIVEN** MCP server is running and the tool exists
- **WHEN** callTool is called with the tool name and parameters
- **THEN** Returns the result of the tool execution

### Requirement: MCPHub

The API SHALL support `MCP getCriticalHubs` to [partial spec — file too large to fully analyze (4 parts)] get critical hubs in the codebase.

**Response:**

```json
{
  "criticalHubs": "array"
}
```

#### Scenario: GetCriticalHubs
- **GIVEN** MCP server is running
- **WHEN** getCriticalHubs is called
- **THEN** Returns a list of critical hubs

### Requirement: MCPFile

The API SHALL support `MCP getFileDependencies` to [partial spec — file too large to fully analyze (4 parts)] get file dependencies for a given file.

**Request:**

```json
{
  "filePath": "string"
}
```

**Response:**

```json
{
  "dependencies": "array"
}
```

#### Scenario: GetFileDependencies
- **GIVEN** MCP server is running and the file exists
- **WHEN** getFileDependencies is called with the file path
- **THEN** Returns a list of file dependencies

### Requirement: MCPCode

The API SHALL support `MCP searchCode` to [partial spec — file too large to fully analyze (4 parts)] search for code in the codebase.

**Request:**

```json
{
  "query": "string"
}
```

**Response:**

```json
{
  "results": "array"
}
```

#### Scenario: SearchCode
- **GIVEN** MCP server is running
- **WHEN** searchCode is called with the query
- **THEN** Returns a list of code search results

### Requirement: MCPSpecificationDomain

The API SHALL support `MCP listSpecDomains` to [partial spec — file too large to fully analyze (4 parts)] list available specification domains.

**Response:**

```json
{
  "domains": "array"
}
```

#### Scenario: ListSpecificationDomains
- **GIVEN** MCP server is running
- **WHEN** listSpecDomains is called
- **THEN** Returns a list of available specification domains

### Requirement: MCPOrientation

The API SHALL support `MCP orient` to [partial spec — file too large to fully analyze (4 parts)] orient the codebase to a given specification.

**Request:**

```json
{
  "specId": "string"
}
```

**Response:**

```json
{
  "orientation": "object"
}
```

#### Scenario: OrientCodebase
- **GIVEN** MCP server is running and the specification exists
- **WHEN** orient is called with the specification ID
- **THEN** Returns the orientation details

### Requirement: MCPArchitecture

The API SHALL support `MCP getArchitectureOverview` to [partial spec — file too large to fully analyze (4 parts)] get an overview of the codebase architecture.

**Response:**

```json
{
  "overview": "object"
}
```

#### Scenario: GetArchitectureOverview
- **GIVEN** MCP server is running
- **WHEN** getArchitectureOverview is called
- **THEN** Returns the architecture overview

### Requirement: MCPFunctionSignature

The API SHALL support `MCP getSignatures` to [partial spec — file too large to fully analyze (4 parts)] get function signatures for the codebase.

**Response:**

```json
{
  "signatures": "array"
}
```

#### Scenario: GetFunctionSignatures
- **GIVEN** MCP server is running
- **WHEN** getSignatures is called
- **THEN** Returns a list of function signatures

### Requirement: MCPSpecificationDrift

The API SHALL support `MCP checkSpecDrift` to [partial spec — file too large to fully analyze (4 parts)] check for specification drift in the codebase.

**Response:**

```json
{
  "drift": "object"
}
```

#### Scenario: CheckSpecificationDrift
- **GIVEN** MCP server is running
- **WHEN** checkSpecDrift is called
- **THEN** Returns the specification drift details

### Requirement: MCPArchitecturalDecision

The API SHALL support `MCP getDecisions` to [partial spec — file too large to fully analyze (4 parts)] get architectural decisions for the codebase.

**Response:**

```json
{
  "decisions": "array"
}
```

#### Scenario: GetArchitecturalDecisions
- **GIVEN** MCP server is running
- **WHEN** getDecisions is called
- **THEN** Returns a list of architectural decisions
