# Verifier Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 23:11
> Source files: src/core/verifier/verification-engine.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Verifies the accuracy of OpenSpec
specifications against the actual code implementation.

## Entities

### VerificationCandidate

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a file candidate for
verification in the system.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Relative path of the file |
| absolutePath | string | Absolute path of the file |
| domain | string | Domain the file belongs to |
| usedInGeneration | boolean | Whether the file was used in generation |
| complexity | number | Complexity score of the file |
| lines | number | Number of lines in the file |
| imports | number | Number of imports in the file |
| exports | number | Number of exports in the file |

### FilePrediction

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the LLM's prediction about a
file's purpose and structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predictedPurpose | string | Predicted purpose of the file |
| predictedImports | string[] | Predicted imports of the file |
| predictedExports | string[] | Predicted exports of the file |
| predictedLogic | string[] | Predicted logic patterns in the file |
| relatedRequirements | string[] | Requirements related to the file |
| confidence | number | Confidence score of the prediction |
| reasoning | string | Reasoning behind the prediction |

### PurposeMatch

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the match result between
predicted and actual file purpose.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predicted | string | Predicted purpose |
| actual | string | Actual purpose |
| similarity | number | Similarity score between predicted and actual |

### SetMatch

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the match result between
predicted and actual sets of imports/exports.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predicted | string[] | Predicted set of items |
| actual | string[] | Actual set of items |
| precision | number | Precision score |
| recall | number | Recall score |
| f1Score | number | F1 score |

### RequirementCoverage

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the coverage of requirements
by a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| relatedRequirements | string[] | Requirements related to the file |
| actuallyImplements | string[] | Requirements actually implemented by the file |
| coverage | number | Coverage percentage |

### VerificationResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the verification result for a
single file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | Path of the verified file |
| domain | string | Domain of the verified file |
| purposeMatch | PurposeMatch | Purpose match result |
| importMatch | SetMatch | Import match result |
| exportMatch | SetMatch | Export match result |
| requirementCoverage | RequirementCoverage | Requirement coverage result |
| overallScore | number | Overall verification score |
| llmConfidence | number | Confidence score from LLM |
| feedback | string[] | Feedback messages |

### DomainBreakdown

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the breakdown of verification
results by domain.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | Domain name |
| specPath | string | Path to the domain specification |
| filesVerified | number | Number of files verified in the domain |
| averageScore | number | Average verification score for the domain |
| weakestArea | string | Weakest area in the domain |

### SuggestedImprovement

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a suggested improvement for a
domain.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | Domain name |
| issue | string | Identified issue |
| suggestion | string | Suggested improvement |

### VerificationReport

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the complete verification
report for the system.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| timestamp | string | Timestamp of the report |
| specVersion | string | Version of the specifications |
| sampledFiles | number | Number of files sampled |
| passedFiles | number | Number of files that passed verification |
| overallConfidence | number | Overall confidence score |
| domainBreakdown | DomainBreakdown[] | Breakdown by domain |
| commonGaps | string[] | Common gaps identified |
| recommendation | 'ready' | 'needs-review' | 'regenerate' | Recommendation for the specifications |
| suggestedImprovements | SuggestedImprovement[] | Suggested improvements |
| results | VerificationResult[] | Verification results for each file |

### VerificationEngineOptions

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the options for the
verification engine.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the project |
| openspecPath | string | Path to the OpenSpec directory |
| outputDir | string | Output directory for reports |
| minComplexity | number | Minimum complexity (lines) for candidate files |
| maxComplexity | number | Maximum complexity (lines) for candidate files |
| filesPerDomain | number | Number of files to sample per domain |
| passThreshold | number | Passing threshold for overall score |
| generationContext | string[] | Files used in generation (to exclude) |

### SpecVerificationEngine

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Engine for verifying that specifications
accurately describe the codebase by predicting code behavior and comparing it to actual files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| llm | LLMService | Service for interacting with a language model |
| options | Required<VerificationEngineOptions> | Configuration options for the verification engine |
| specs | LoadedSpec[] | Array of loaded specifications |
| parser | ImportExportParser | Parser for analyzing import/export statements |

**Relationships:**

- uses LLMService (Uses LLMService to generate predictions about code behavior)
- uses ImportExportParser (Uses ImportExportParser to analyze file imports and exports)
- uses DependencyGraphResult (Uses DependencyGraphResult to select verification candidates)

## Requirements

### Requirement: VerificationCandidateValidation

The system SHALL validate VerificationCandidate according to these rules:
- path must be a valid file path
- absolutePath must be a valid absolute path
- complexity must be a positive number
- lines must be a positive integer
- imports must be a non-negative integer
- exports must be a non-negative integer

#### Scenario: CreateVerificationCandidate
- **GIVEN** A file path and domain information
- **WHEN** VerificationCandidate is instantiated
- **THEN** A new VerificationCandidate object is created with the provided properties

### Requirement: FilePredictionValidation

The system SHALL validate FilePrediction according to these rules:
- confidence must be between 0 and 1
- predictedImports must be an array of strings
- predictedExports must be an array of strings
- predictedLogic must be an array of strings
- relatedRequirements must be an array of strings

#### Scenario: GenerateFilePrediction
- **GIVEN** A file path and OpenSpec specifications
- **WHEN** LLMService generates a prediction
- **THEN** A FilePrediction object is created with the predicted properties

### Requirement: PurposeMatchValidation

The system SHALL validate PurposeMatch according to these rules:
- similarity must be between 0 and 1

#### Scenario: CalculatePurposeSimilarity
- **GIVEN** Predicted and actual purpose strings
- **WHEN** PurposeMatch is instantiated
- **THEN** A PurposeMatch object is created with the calculated similarity score

### Requirement: SetMatchValidation

The system SHALL validate SetMatch according to these rules:
- precision must be between 0 and 1
- recall must be between 0 and 1
- f1Score must be between 0 and 1

#### Scenario: CalculateSetMatchMetrics
- **GIVEN** Predicted and actual sets of items
- **WHEN** SetMatch is instantiated
- **THEN** A SetMatch object is created with the calculated precision, recall, and F1 score

### Requirement: RequirementCoverageValidation

The system SHALL validate RequirementCoverage according to these rules:
- coverage must be between 0 and 100

#### Scenario: CalculateRequirementCoverage
- **GIVEN** Related and implemented requirements
- **WHEN** RequirementCoverage is instantiated
- **THEN** A RequirementCoverage object is created with the calculated coverage percentage

### Requirement: VerificationResultValidation

The system SHALL validate VerificationResult according to these rules:
- overallScore must be between 0 and 100
- llmConfidence must be between 0 and 1

#### Scenario: GenerateVerificationResult
- **GIVEN** Verification data for a file
- **WHEN** VerificationResult is instantiated
- **THEN** A VerificationResult object is created with the provided properties

### Requirement: DomainBreakdownValidation

The system SHALL validate DomainBreakdown according to these rules:
- filesVerified must be a positive integer
- averageScore must be between 0 and 100

#### Scenario: GenerateDomainBreakdown
- **GIVEN** Verification results for a domain
- **WHEN** DomainBreakdown is instantiated
- **THEN** A DomainBreakdown object is created with the calculated properties

### Requirement: VerificationReportValidation

The system SHALL validate VerificationReport according to these rules:
- sampledFiles must be a positive integer
- passedFiles must be a non-negative integer
- overallConfidence must be between 0 and 1
- recommendation must be one of 'ready', 'needs-review', or 'regenerate'

#### Scenario: GenerateVerificationReport
- **GIVEN** Verification results and domain breakdowns
- **WHEN** VerificationReport is instantiated
- **THEN** A VerificationReport object is created with the provided properties

### Requirement: VerificationEngineOptionsValidation

The system SHALL validate VerificationEngineOptions according to these rules:
- minComplexity must be a positive number if provided
- maxComplexity must be a positive number if provided
- filesPerDomain must be a positive integer if provided
- passThreshold must be between 0 and 100 if provided

#### Scenario: ConfigureVerificationEngine
- **GIVEN** Verification engine options
- **WHEN** VerificationEngine is instantiated
- **THEN** A VerificationEngine object is created with the provided options

### Requirement: SpecVerificationEngineValidation

The system SHALL validate SpecVerificationEngine according to these rules:
- Must have valid LLMService instance
- Options must include rootPath and openspecPath
- Specs must be loaded before verification can begin
- Verification candidates must be selected before verification

#### Scenario: VerifyCodebaseAgainstSpecifications
- **GIVEN** A codebase with specifications
- **WHEN** verify() is called with a dependency graph and spec version
- **THEN** A verification report is generated and saved
- **AND** All specifications are loaded
- **AND** Verification candidates are selected
- **AND** Each candidate file is verified
- **AND** A detailed report is generated and saved

#### Scenario: LoadSpecifications
- **GIVEN** A directory containing specifications
- **WHEN** loadSpecs() is called
- **THEN** All specifications are loaded into memory
- **AND** Specifications are stored in the specs array
- **AND** Each specification includes domain, path, and content

#### Scenario: SelectVerificationCandidates
- **GIVEN** A dependency graph
- **WHEN** selectCandidates() is called
- **THEN** Candidate files are selected for verification
- **AND** Files are grouped by domain
- **AND** Files are sorted by connectivity
- **AND** A specified number of files per domain are selected

#### Scenario: VerifyASingleFile
- **GIVEN** A verification candidate
- **WHEN** verifyFile() is called
- **THEN** The file is verified against its specifications
- **AND** A prediction is generated using the LLM
- **AND** The file content is analyzed
- **AND** The prediction is compared to the actual file content
- **AND** A verification result is returned

#### Scenario: GenerateAVerificationReport
- **GIVEN** Verification results
- **WHEN** generateReport() is called
- **THEN** A comprehensive verification report is generated
- **AND** Summary statistics are calculated
- **AND** Domain breakdown is generated
- **AND** Common gaps are identified
- **AND** Suggested improvements are provided

### Requirement: VerifySpecifications

> Implementation: `src/api/index.ts:5` · confidence: semantic

The system SHALL compares openspec specifications with actual code implementation to verify accuracy.

#### Scenario: SuccessfulVerification
- **GIVEN** Valid OpenSpec specifications and matching code implementation
- **WHEN** verifySpecifications is called
- **THEN** A VerificationReport is generated with high scores and 'ready' recommendation

#### Scenario: PartialVerification
- **GIVEN** OpenSpec specifications with some mismatches in code implementation
- **WHEN** verifySpecifications is called
- **THEN** A VerificationReport is generated with medium scores and 'needs-review' recommendation

#### Scenario: FailedVerification
- **GIVEN** OpenSpec specifications with significant mismatches in code implementation
- **WHEN** verifySpecifications is called
- **THEN** A VerificationReport is generated with low scores and 'regenerate' recommendation

### Requirement: GenerateVerificationReport

> Implementation: `src/api/index.ts:4` · confidence: semantic

The system SHALL generates a detailed report comparing openspec specifications with actual code implementation.

#### Scenario: CompleteReportGeneration
- **GIVEN** Array of VerificationResult objects
- **WHEN** generateVerificationReport is called
- **THEN** A comprehensive VerificationReport is generated

### Requirement: AnalyzeFilePrediction

> Implementation: `src/core/analyzer/code-shaper.ts:24` · confidence: semantic

The system SHALL analyzes the llm prediction for a file and compares it with the actual code implementation.

#### Scenario: MatchingPrediction
- **GIVEN** FilePrediction matches the actual code implementation
- **WHEN** analyzeFilePrediction is called
- **THEN** A VerificationResult with high scores is generated

#### Scenario: PartiallyMatchingPrediction
- **GIVEN** FilePrediction partially matches the actual code implementation
- **WHEN** analyzeFilePrediction is called
- **THEN** A VerificationResult with medium scores is generated

#### Scenario: NonMatchingPrediction
- **GIVEN** FilePrediction does not match the actual code implementation
- **WHEN** analyzeFilePrediction is called
- **THEN** A VerificationResult with low scores is generated

### Requirement: Verify

> Implementation: `src/api/index.ts:5` · confidence: heuristic

The system SHALL runs the full verification process, loading specifications, selecting candidate files, verifying each file, and generating a verification report.

#### Scenario: SuccessfulVerification
- **GIVEN** Valid dependency graph and specifications
- **WHEN** verify is called
- **THEN** A verification report is generated and saved

#### Scenario: NoSpecificationsFound
- **GIVEN** No specifications are available
- **WHEN** verify is called
- **THEN** An error is thrown indicating no specs were found

#### Scenario: NoVerificationCandidates
- **GIVEN** No suitable verification candidates are found
- **WHEN** verify is called
- **THEN** An error is thrown indicating no candidates were found

### Requirement: LoadSpecs

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL loads all specifications from the openspec directory.

#### Scenario: SpecsLoadedSuccessfully
- **GIVEN** Specifications directory exists and contains valid specs
- **WHEN** loadSpecs is called
- **THEN** Specifications are loaded into the engine

#### Scenario: SpecsDirectoryDoesNotExist
- **GIVEN** Specifications directory does not exist
- **WHEN** loadSpecs is called
- **THEN** No specifications are loaded

### Requirement: SelectCandidates

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL selects candidate files for verification based on the dependency graph and configuration options.

#### Scenario: CandidatesSelectedSuccessfully
- **GIVEN** Dependency graph contains suitable files
- **WHEN** selectCandidates is called
- **THEN** A list of verification candidates is returned

#### Scenario: NoSuitableCandidates
- **GIVEN** Dependency graph contains no suitable files
- **WHEN** selectCandidates is called
- **THEN** An empty list is returned

### Requirement: VerifyFile

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL verifies a single file by comparing the llm prediction to the actual file content.

#### Scenario: FileVerifiedSuccessfully
- **GIVEN** Valid verification candidate
- **WHEN** verifyFile is called
- **THEN** A verification result is returned

#### Scenario: FileVerificationFails
- **GIVEN** Invalid verification candidate
- **WHEN** verifyFile is called
- **THEN** An error is thrown indicating verification failure

### Requirement: GetPrediction

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL gets a prediction from the llm for a given file.

#### Scenario: PredictionSuccessful
- **GIVEN** Valid verification candidate
- **WHEN** getPrediction is called
- **THEN** A file prediction is returned

#### Scenario: PredictionFails
- **GIVEN** Invalid verification candidate
- **WHEN** getPrediction is called
- **THEN** A default prediction with low confidence is returned

### Requirement: ComparePurpose

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL compares the predicted purpose of a file to its actual purpose.

#### Scenario: PurposeMatchSuccessful
- **GIVEN** Valid predicted and actual purposes
- **WHEN** comparePurpose is called
- **THEN** A purpose match result is returned

#### Scenario: PurposeMatchFails
- **GIVEN** Invalid predicted or actual purposes
- **WHEN** comparePurpose is called
- **THEN** A purpose match result with low similarity is returned

### Requirement: CompareImports

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL compares the predicted imports of a file to its actual imports.

#### Scenario: ImportMatchSuccessful
- **GIVEN** Valid predicted and actual imports
- **WHEN** compareImports is called
- **THEN** A set match result is returned

#### Scenario: ImportMatchFails
- **GIVEN** Invalid predicted or actual imports
- **WHEN** compareImports is called
- **THEN** A set match result with low precision and recall is returned

### Requirement: CompareExports

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL compares the predicted exports of a file to its actual exports.

#### Scenario: ExportMatchSuccessful
- **GIVEN** Valid predicted and actual exports
- **WHEN** compareExports is called
- **THEN** A set match result is returned

#### Scenario: ExportMatchFails
- **GIVEN** Invalid predicted or actual exports
- **WHEN** compareExports is called
- **THEN** A set match result with low precision and recall is returned

### Requirement: AnalyzeRequirementCoverage

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL analyzes the coverage of requirements in a file.

#### Scenario: RequirementCoverageSuccessful
- **GIVEN** Valid related requirements and file content
- **WHEN** analyzeRequirementCoverage is called
- **THEN** A requirement coverage result is returned

#### Scenario: RequirementCoverageFails
- **GIVEN** Invalid related requirements or file content
- **WHEN** analyzeRequirementCoverage is called
- **THEN** A requirement coverage result with low coverage is returned

### Requirement: GenerateReport

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL generates a verification report based on the verification results.

#### Scenario: ReportGeneratedSuccessfully
- **GIVEN** Valid verification results
- **WHEN** generateReport is called
- **THEN** A verification report is returned

#### Scenario: ReportGenerationFails
- **GIVEN** Invalid verification results
- **WHEN** generateReport is called
- **THEN** An error is thrown indicating report generation failure

### Requirement: SaveReport

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL saves the verification report to the specified output directory.

#### Scenario: ReportSavedSuccessfully
- **GIVEN** Valid verification report
- **WHEN** saveReport is called
- **THEN** The report is saved to the output directory

#### Scenario: ReportSaveFails
- **GIVEN** Invalid verification report
- **WHEN** saveReport is called
- **THEN** An error is thrown indicating save failure

### Requirement: GetDomains

> Implementation: `src/core/generator/index.ts:1` · confidence: heuristic

The system SHALL gets the list of domains from the loaded specifications.

#### Scenario: DomainsRetrievedSuccessfully
- **GIVEN** Specifications are loaded
- **WHEN** getDomains is called
- **THEN** A list of domains is returned

#### Scenario: DomainsRetrievalFails
- **GIVEN** Specifications are not loaded
- **WHEN** getDomains is called
- **THEN** An empty list is returned

## Technical Notes

- **Implementation**: `src/core/verifier/verification-engine.ts`
- **Dependencies**: LLMService, DependencyGraphResult, DependencyNode, ImportExportParser

## Dependencies

### Called by this domain
- `api` → `VerificationReport`, `SpecVerificationEngine`
- `commands` → `SpecVerificationEngine`, `VerificationReport`, `VerificationResult`

### Calls into
- `analyzer` → `ImportExportParser`, `DependencyGraphResult`, `DependencyNode`
- `services` → `LLMService`
- `utilities` → `logger`
